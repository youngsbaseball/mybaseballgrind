# 🛣️ My Grind — Roadmap

*Re-prioritize this file at the start of every week. Move shipped items to the bottom and into STATUS.md.*

---

## 🔥 This Week (pick 1 per session)

### P0 — queue (refreshed 2026-08-12 — **Autopsy day: the Bash-hang root cause found (**NOT FIXED — the 8/13 run was one good run, not a cure.** DISPROVEN 2026-08-16: tasks all fired on time and Chrome's AX tree was verified alive, and the 7:08 run still produced nothing. Four straight dark days 8/14-8/17. Cause is isolated to the scheduled-task session; instrument it, do not restart again).** 1 commit `abc9e56` (the 8/12 card pair). **(1) STORAGE UNIT WRITTEN NOTICE — 8/13 IS THE LAST DAY.** Two-line email, fully out before 8/28. **(2) PUBLISH THE STACKED META DRAFTS** — Meta has been dark since 8/8 7:30 PM; drafts for 8/12 ("School started this week") and 8/13 ("Thursday drill tip") sit in Content > Drafts needing card attach + 7:30 PM on both rows + Schedule (machine attach blocked by the dead AX tree — a Mac reboot is the only known cure; hand-attach otherwise). **(3) REGGIE TAIL:** Fri 8/14 5:30 AM non-opener resend fires on its own (~56 pool); ceremony Sat 8/15 — day-of push. **(4) Vercel re-login** + backfill the 8/12-8/13 row fields (first days of blog-tag data). **(5) Books:** booster-meeting outcome + the last-week draw amount still owed to the 📒 log; ~$60 in the business account — check autopays. **(6) Jaime Rangel's trial converts ~8/17** — personal heads-up this weekend. **(7) C&P exam Wed 8/19 2:40 PM Thousand Oaks** — square the school early-leave. ——— PREVIOUS P0 — queue (refreshed 2026-08-11 — **Newsletter out, pipeline down.** 1 commit `847f3fe` (Vercel Analytics tag on blog + public pages). **(1) AUTOPSY THE 7:08 DAILY SOCIAL TASK** — it silently produced NOTHING 8/10 + 8/11 on BOTH channels (no content folders, no Meta schedules, no X posts) while reporting normal run timestamps; catch tomorrow's 7:08 run live or read its output; the Reggie tail runs through Sat 8/15 and every dark evening costs reach. **(2) STORAGE UNIT WRITTEN NOTICE — window closes ~8/13.** Two-line email, fully out before 8/28. **(3) The Wed 8/12 12:00 PM rescued post fires on its own** (No-Grades baseball card, girls-baseball lead) — verify it published; the 7:30 evening slot belongs to Wednesday's daily post once the task is fixed. **(4) WEDNESDAY = OPEN-RATE MORNING** — pull the Reggie issue's opens/clicks into Daily Numbers; the Fri 8/14 non-opener resend is armed. **(5) Stripe + Mailchimp connector re-auth** (claude.ai connector settings) — paid counts carried since 8/8. **(6) SSA 11:30 outcome → memory** (survivor-at-60 vs own-record numbers; capture into ssa-survivor-benefits-claim). **(7) C&P exam Wed 8/19 2:40 PM Thousand Oaks** — square the school early-leave this week. **(8) FB inbox: 1 unread message + 2 comments parked** (hide any AI callouts, never reply defensively). ——— PREVIOUS P0 — queue (refreshed 2026-08-03 — **Big build day, 7 commits.** **(1) THE NEWSLETTER IS ONE CLICK FROM SENDING** — Issue #14 "Did they actually rest?" is 5/5 complete in Mailchimp but STILL A DRAFT; Coach clicks Schedule (Tue 8/4 5:30 AM PT, auto-resend to non-openers Fri 8/7). Everything downstream depends on this. **(2) SCHEDULE THE META POST ~8:00 AM Tue 8/4** — Pattern A cover cards (1080x1350 / 1080x1080 / 1080x1920) + CAPTIONS.md already sit in Drive "03 — Marketing & Social/Social Content/2026-08-04 Newsletter — Back To School"; deliberately NOT scheduled the night before because the caption asserts the send happened. Reminder fires 7:30 AM. **(3) AMAZON ARM CARE LIST HAS ONLY 2 ITEMS** and arm care is the exact topic of the issue landing that morning — fill it before the traffic arrives. **(4) HARD DATE: CalVet Tue 8/4 9:30 AM**, SCV Veterans Services Office, bring the thumb drive. **(5) PHASE A DAY-3 READ Thu 8/6 9 AM** (armed task) — check paid-session engagement for the 0-second junk signature; Meta force-applied Advantage+ expansion so placements/geo/age are the only hard levers. **(6) COACH-SEND QUEUE:** Trinity confirmation text + AD email to Ken (review the "black out days" wording) + Trinity head coach name/contact for the Offers Tracker. **(7) TRYOUTS POST DISTRIBUTION** — the FB page post is live; groups, IG feed, Reel, and TikTok are phone-only and still owed. **(8) THE 7/24 SIGNUP BREAK, carried a fifth time** — check what shipped 7/23-7/24 and whether the funnel changed. **(9) BLACK BOOK WAVE 1 TAIL + CANYON DOC SHARING** — Share → Commenter for canyoncbb@gmail.com still blocks the booster email. ——— PREVIOUS P0 — queue (refreshed 2026-08-02 — **Hold lifted, Phase A live.** 1 docs commit `8ef722c`. **(1) BUILD THE 8/4 NEWSLETTER MONDAY** — Tuesday is the 5:30 AM send AND CalVet at 9:30 AM, so the HTML + settings block must be in Coach's hands Monday; deploy any linked blog post first. **(2) JEFF ZACHA TRIAL CLIFF 8/4** — first card-required conversion test; watch Stripe for the charge or the pause. **(3) PHASE A SELF-LAUNCHES Mon 8/3 2:46 PM** (campaign "MyGrind Academics Aug 2026", ad account 302225839838083, AN unchecked); no action until the armed day-3 read task fires Thu 8/6 9 AM. **(4) THE 7/24 SIGNUP BREAK, carried a fourth time** — check what shipped 7/23-7/24 and whether the funnel changed. **(5) BLACK BOOK WAVE 1 TAIL + CANYON DOC SHARING** — Coach sends the 3 remaining drafts; Share → Commenter for canyoncbb@gmail.com on both Canyon docs still blocks the booster email. ——— PREVIOUS P0 — queue (refreshed 2026-07-29 — **Black Book goes to market.** 0 commits here; 2 to coachbook. **(1) THE 7/24 SIGNUP BREAK, carried twice, is tomorrow's start.** Six days without a signup. Traffic was already low and flat when it broke, so it is NOT a traffic story: check what shipped 7/23-7/24 and whether the funnel changed. **(2) DIAGNOSE THE GIT HANG IN THIS REPO** — every git command stalls past timeout, two nights running, no index.lock present; the 7/28+7/29 STATUS/ROADMAP edits are stacking uncommitted in the working tree. **(3) BLACK BOOK WAVE 1 TAIL:** Coach sends the 3 remaining drafts (Fischenich, Skinner, Fournier→Weber); print the one-pager (Drive 03 — Marketing & Social) for Franchimone/SCCS (warmest walk-in), Heritage, Vasquez, Burroughs, Palmdale + fall-game handoffs. LOCKED: founder-led direct only, no paid; App Store parked until 10-15 paying books. **(4) CANYON DOC SHARING still blocks the booster email** — Coach: Share → Commenter for canyoncbb@gmail.com on BOTH the sponsorship form and the funding plan. (5) Update mygrind-daily-social-post SKILL.md with the Chrome fix: picker refuses until Chrome restarts, and restart reopens the wrong profile — relaunch with --profile-directory="Profile 1". ——— PREVIOUS P0 — queue (refreshed 2026-07-28 — **The ads were never working.** 1 commit `f1481dc`, READY. **(1) PAID ADS ARE ON HOLD, INDEFINITE.** Phase A stays an unpublished draft; the Aug 3 start does not happen. Do NOT resume paid without Coach saying so explicitly. Nothing is spending, so nothing needs pausing. **(2) THE REAL QUESTION IS THE 7/24 SIGNUP BREAK** — signups ran ~1 every 2 days through mid-July and stopped cold on 7/24. That break happened while traffic was already low and flat, so it is NOT a traffic story. Check what shipped 7/23-7/24 and whether anything changed in the funnel. **(3) CANYON SPONSORSHIP FORM — finish it.** Reminder `canyon-sponsorship-form-followup` fires 7/29 9:00 AM. Blocking step first: **both Google Docs are still private**, so the booster email to canyoncbb@gmail.com would land as "request access" — Coach must Share → Commenter on BOTH docs (no connected tool can do this). Then three TBD fields (booster legal name, EIN, who receives forms/payment) and the deferred decision on a renewal discount ladder (baking one in means editing the drafted email, which currently asks the board that question). **(4) SEND THE 2 BLACK BOOK LETTERS** — drafted in Gmail to Luke Barrett (Grace) and Juan Velazquez (Campbell Hall); Oak Park + Nordhoff intentionally NOT drafted because the only emails on file are ADs, not coaches. NEW LOCKED DECISION: paid ads held; `meta / paid` was 651 sessions at 0s engagement and 0 key events, $241.53/30d for zero revenue, and signups proved uncorrelated with traffic volume (490 visitors/wk → 3 signups; 100-150/wk → 5). CARRIED: `coach.html` still untracked; HP Instant Ink $6.03 dispute blocked on Coach's cancellation date + logins. ——— PREVIOUS P0 — queue (refreshed 2026-07-27 — **The ads told the truth + She Plays.** 12 commits, all READY. (1) **FINISH PHASE A** — the draft campaign is built in Ads Manager but unpublished. Two steps: set **Manual placements** (FB Feed + Stories, IG Feed + Stories, **Audience Network fully unchecked**) then build the 3 ads from `media/ads/2026-08-academics/` with the three distinct `utm_content` URLs and CTA 'Sign up'. Budget approved at **$140, Aug 3-16, $10/day**. Blocker: the permission classifier refused campaign schedule fields, so either grant it or fill by hand. (2) **SEND OR SCHEDULE THE 7/28 NEWSLETTER** — it is loaded in Mailchimp and PAUSED. Coach's own subject is in place. Send a test and click the new blog link first (Mailchimp rewrites URLs with query strings). (3) **X POST FOR 7/27 DID NOT GO OUT** — draft saved, card not attached, Chrome AX tree died. Restart Chrome before the next daily social run. (4) **READ THE PHASE A BAR ON THE RIGHT METRIC** — day-7 check is **GA4 average engagement time on `meta / paid`, must clear 15s**, not Meta's reported conversions. Day-14 kill at >$35 per trial start. NEW LOCKED DECISIONS: Fall Ball = a new year not tryouts; MyGrind is national, never geo-narrow ads to an SCV radius; Phase A runs Traffic not Sales. ——— PREVIOUS P0 — queue (refreshed 2026-07-22 — **The Signal runs the hire + two bad metrics killed.** 3 commits `eff11b0`/`570ac56`/`cdefd7c`, all READY. (1) PRESS/E-E-A-T: hire published by The Signal 7/22; founder schema now carries the head-coach role + affiliation + the article as subjectOf/NewsArticle, on BOTH index.html and foundermygrind.html (same #founder @id); 14 blog posts inherit by @id. (2) META AUTO-POST FIXED AT THE ROOT: 7/21 never failed to publish, it was never scheduled — Chrome will not open a native file picker for a BACKGROUND tab. Tab-activation + sheet verification + Planner verify + segmented-time fix + X cmd+Return all written into the task. (3) FIREBASE 'Signed In' IS NOT USAGE — journal is local-first, that column tracks cloud-sync adoption only; managed minors have no Auth identity. Kelly was active all along. NEW FEATURE from that correction: api/cron/backup-nudge.js (Thursdays, targets only accounts with zero cloud entries, self-extinguishes on first sync, 3-send cap, free AND paid, fails safe) + /signin.html?next=backup deep link. **DRY-RUN BY DEFAULT — add ?live=1 to the cron path to actually send.** NEXT (Thu 7/23): storage unit is the money item — renews the 28th, notice-period question open since 7/20, daily nudge at 9am. Then: Marc Overdevest's email -> send the CIF compliance question (drafted, unblocked by nothing else). COACH OWES: Mailchimp re-login (2 days owed, blocks the 7/21 open-rate read + Friday's resend verify). Fri 7/24: ad read #2 = stop-rule decision + homepage bounce read (UNCHANGED). ——— PREVIOUS 2026-07-17 — **Week-read verdict + homepage cut in half + next week built.** 4 commits `932c4e7`/`ab76894`/`0d6ae68`/`ec4a136`, all READY. AD READ (post-7/12): $54.44, $0.67 CPC, 3.23% CTR, 65 LPVs $0.84, 0 ad-attributed checkouts (Kelly organic) -> walkthrough_v1 ADDED to the live Sales ad set (not swapped; the staged copy was in the ENDED 30D ad set, duplicated over); STOP RULE: 0 checkouts by 7/24 read = pause. TEAMS-FIRST locked (all 6 customers warm, 0 from ads; D1GRIND 250 slots + Mon 7/20 opening = the engine; 15-20 founders by Labor Day checkpoint). Homepage: 11 -> ~7.7 phone screens (screenshot carousel + FAQ 4+reveal + ONE headline: 'Log it. Own it.' is the H1, Track Your Grind retired everywhere incl. all 8 OG cards). Arm care blog post + 7/21 newsletter (hero-card standard, The Arm + J-Bands picks) DEPLOYED. Reel live FB+IG. Chrome fix permanent (YBG profile). VU match portable in writing (same price or higher; lower-price question sent). NEXT (Mon 7/20): affiliate links -> Mailchimp paste for Tue 5:30 AM; D1 grand opening w/ real footage (11 AM Encino lesson same day); Kelly nudge if still no sign-in; signs-sold + VA claim Monday checks. Fri 7/24: ad read #2 = the stop-rule decision + homepage bounce read. ——— PREVIOUS 2026-07-12 — **Ad tracking fixed at the root.** 1 commit `122b2a0`. Day-3 read: Meta pixel NEVER received InitiateCheckout/CompleteRegistration from real visitors (tracker blocking) — Conversions API now server-side (lib/meta-capi.js, event_id dedup, _fbp/_fbc attribution; client conversion fbq removed, never re-add), verified to prod log + Events Manager; LOCKED: judge campaign on post-7/12 data. Walkthrough now on TikTok + Shorts too (every surface but @youngsbaseball IG). Grammarly cancelled + $144 refunded (settle 7/15); HomeGoods $27.43 logged; DBA/FBN county-filed + archived, renews 2/26/2031. NEXT (7/13): first full CAPI day read, NO ad edits until week-read ~7/17 then walkthrough_v1 ad; Coach: @youngsbaseball reel + Reddit account (carried 3 days). ——— PREVIOUS 2026-07-11 — **Sport unlock + walkthrough lever closed.** 4 commits, SW v395→v398, all READY. Both sports open to every account (gate removed, LOCKED: never re-add); D1 Northridge grand opening Mon 7/20 promoted in-app (self-expiring block, light-theme hex fix v398); Coach's real 21s walkthrough: homepage muted-loop embed LIVE (91%-bounce answer) + 28s social cut (Higgsfield hook + end card) published to FB page/Story + @mygrindapp; SW media-bypass fix v397 (never intercept Range/video). NEW RULE: #journal #accountability on every ad/post. NEXT (7/12): Meta day-3 read (key events vs InitiateCheckout, NO edits); post walkthrough to @youngsbaseball IG + TikTok + Shorts from Drive; Reddit account still open; walkthrough_v1 ad after week read ~7/17. ——— PREVIOUS 2026-07-09 — **Full audit worked in one day + silent-save hotfix + ad relaunch published.** 8 commits, SW v389→v394, 6 deploys all READY. AM: Buckets 1+2 (48 fixes: paywall trio, chronological journal sort, stats correctness incl. softball per-7, trial reminder email live on the webhook, CALIFORNIAMYGRIND). PM: Bucket 4 softball-feel (sport-state root cause: boot follows family sport, toggle unlock, kid-switch profile sport; Softball Pathway card; per-sport workout videos) + Bucket 3 complete (-730 lines dead code, tokenized toasts, stale-comment sweep) + HOTFIX for the since-5/11 silent-save crash (entries saved, confirmation never showed) + ads rebuilt (ViewContent/InitiateCheckout pixel events, card pre-framed on the gate, Sales campaign published at $10/day on InitiateCheckout, enhancements ON per new rule, translation off). NEXT (7/10): Coach records the 30s walkthrough video → walkthrough_v1 ad + it doubles as the 'what do I tap' onboarding answer; morning-sync reads first delivery numbers; consider the first-run 3-tap spotlight (Log→Stat→Goal). Audit deferrals: item 30 invite queue (own session), item 52 mg-config, share-modal dedup. ——— PREVIOUS 2026-07-02 — **Funnel sealed on all paths + onboarding white & audited + ALLSTARMYGRIND live.** 9 commits, all READY. Self/fallback card-gate bypass closed + dead-since-5/27 checkout-return handler revived (`0573b86`); onboarding white + light logo + first dedicated audit w/ 12 fixes incl. expired-trial revival leak (`35fbed5`/`b3bc4fa`/`ce2c4bf`); ALLSTARMYGRIND live — 90-day server trial, cap 15, NO Stripe coupon by design, offer PDF delivered (`7273f1b`); security backlog closed + live-verified (`b5b8f15`); NCAA SEO push w/ requirements table + exact-match FAQs (`c344e95`); Season Goals + 8 dark siblings converted (`3de9428`). NEXT (7/3): verify first AllStars Trialing signups in Stripe + Uriel's $14.16 payout in Chase ••2761 + Coach's GSC request-index click; then the Meta ad creative when Coach picks footage. Watch: NCAA queries avg position <10 + first clicks (1-3 wks). ——— PREVIOUS 2026-07-01 — **Card-required signup shipped early + 30-day trial + full app audit.** 8 commits to `main`, all READY. The 6/24 no-card leak is CLOSED (`d96a99f`, plan-picker skip deleted, every signup = visible Stripe card-on-file customer; /phase5-preflight passed); trial moved 14→30 in lockstep (`da6a30f` + `e7255e1` straggler sweep incl. a real onboarding 14-day fallback bug); annual price unified to $99.99; coach-only `/api/admin/trial-count` counts the legacy no-card cohort. Full 3-agent audit: security CLEAN (no critical/high). PWA now real: /manifest.json (light splash) + sw.js v389 offline app shell — the journal opens with no signal. Polish: zoom caps removed, signin brand fonts, /media/ cache headers, landing headings --gold-dark, Screen 8 card readable + honest (Coach-feedback tease → Academics & GPA; "Resend setup link" + explainer), review prompt 3→7-day copy + invisible-textarea fix, lead landing screenshot reshot LIGHT (harness at scripts/shoot-light-dashboard.html), welcome email "Payment landed" fix + open-on-your-phone install tip. NEXT (7/2): read day-1 card-required signup data (Vercel signup visits + Firebase identities + new Stripe customers vs baseline) + build the card-required Meta ad creative before ANY ad relaunch (old "no card" creative now mismatches the landing); alt = the security hardening trio (push-subscribe IP rate limit; feedback-request requires auth BEFORE SMS_DRY_RUN flips; delete dead api/magic-link-request.js). Coach decisions open: signin light-vs-dark, "elite" in recruiting features, /links in sitemap. ——— PREVIOUS 2026-06-27 — **GEO E-E-A-T shipped + logo system cleaned + daily social engine.** 1 commit `b826099` (named-author E-E-A-T on all 10 blog posts: "Coach Mike Young, 35 years" byline + Person schema linked to #founder, plus `docs/geo-ai-baseline.md`; deployed). GEO pass: llms.txt/robots verified live; AI-answer baseline recorded (MyGrind ranks for none of the 4 target queries yet). Marketing/ops: first card-on-file conversion (paid 2->3, Uriel); built `mygrind-daily-social-post` (7am daily auto-post + road-to-10k plan); logo audit — Concept 1 (compass-as-G) retired to Desktop `_DO NOT USE`, built+uploaded gold mark `4af5ecc6` + white `7e52c839` to fix the blacked-out-on-dark logo; today's "log it after the game" ad set (baseball + softball stills + testimonial Reels, gold logo on gear) in Drive + Mac Photos. NEW LOCKED RULES: blog-byline E-E-A-T exception; MyGrind logo on hats/helmets/uniforms (all ads); gold/white logo on dark never black; always make BOTH baseball + softball versions; videos ~12s. NEXT (6/28+): finish weighted/plyo-ball Track 1 — 4 demo videos in `softball.html` (oEmbed-verified) + PlyoCare `/picks` gear card once Coach sends the Amazon link; alt = GA4 `purchase`-event fix (background `task_375db48f`). Reminders: FTB 2023 due Mon 6/29; toggle Meta Video ad OFF; Reggie written release before paid use. ——— PREVIOUS 2026-06-24 — **Business-ops + 2 homepage commits.** 2 commits to `main` (SW v385->v387, Coach's own GitHub edits): `92498fb` v386 player-first homepage hero + an inline one-field newsletter email box IN THE HERO (ships the ~6/30-deferred conversion tweak EARLY) + tighter mobile spacing; `9c0c02d` v387 rewrote the homepage Coach note around the 35-year origin story. BUSINESS/OPS (Notion, non-code): overhauled expense tracking into ONE source of truth — the 📥 Expenses DB is now canonical (feeds P&L; was stale since April), logged the 6/24 Amazon DJI Mic 3 + Insta360 order $404.96 on Chase Business debit ...8008 (CONFIRMS the debit is ACTIVE), backfilled 14 expenses, archived 11 personal/non-deductible rows, added CapCut, reconciled Namecheap, confirmed the ~$194/mo software stack (memory notion-expense-tracker). Homepage-conversion: the deferred hero email box SHIPPED — the ~6/30 revisit is now "read the 6/24 ad traffic against the new hero." NEXT (6/25): confirm the 6/24 NCAA IG post + Meta trial14_ab relaunch fired; Thu 6/25 9am GA4 star newsletter_signup + bio_hub_click; alt = fill Pinterest's 5 empty boards / set up Reddit. ——— PREVIOUS 2026-06-23 — **Pinterest channel launched + app polish v383->v385.** 4 commits to `main` (SW v382->v385): the final 4 plyo/weighted-ball drill demos curated + oEmbed-verified (`9eec46f` v383, closing the 6/27 MVP follow-up EARLY — all 8 drills now have a real demo), Journal entry-log polish (`a83bac3` v384), a Tier 2 "Your Last 7 Days" recap strip on the Journal tab (`270b06d` v385), and the Pinterest domain-verification tag (`fa1120b`). BIG NON-CODE: launched the Pinterest channel — rebranded the YBG-era account -> @MyGrindAppOfficial, claimed+verified mygrindapp.com, 8 keyword boards, 10 pins built (nano_banana_pro, warm-dark+gold, UTM-tagged, softball markers) from real blog posts + scheduling ~2-3/day; fixed an AI bat-in-glove artifact on the softball pin. New memories: pinterest-channel-live, pinterest-playbook, daily-social-media-routine, copy-paste-ready-deliverables, mygrind-copy-no-em-dashes. NEXT (6/24): confirm the Meta relaunch fired + post the NCAA "3.7->2.9" IG card 7pm, then set up Reddit (high-trust companion channel); alt = fill the 5 empty Pinterest boards. ——— PREVIOUS 2026-06-22 — **Weighted + plyo ball training MVP (second session).** 4 commits, SW v380->v382: plyo + weighted-ball drills in the Arm Care/Bands workout focus (4 plyo + 4 weighted, lightest->heaviest) + a weighted-ball safety gate + a Coach's Picks gear link; the /picks weighted-ball gear card = Grady's Weighted Velocity Ball Set (seam+seamless, 6 balls, case); 4 of 8 new drills have curated oEmbed-verified demos (other 4 on search fallback). Non-code: non-salesy Baseball Moms reply -> free when-to-switch-travel-teams blog; HS-choice blog + /picks confirmed crawlable (Coach to GSC Request Indexing). REMINDERS: post the 6/23 newsletter Story (Tue 6/23 7:30am); finish deferred MVP items (Sat 6/27 — 4 uncurated drill videos + a PlyoCare /picks card once Coach sends the link). NEXT: monitoring (newsletter 6/23, Meta relaunch 6/24, day-3 read 6/26, NCAA IG 6/24). PREVIOUS same-day — **App polish + Coach's Picks launch.** 17 commits, SW v370->v380. New Coach's Picks page /picks (evergreen affiliate hub) + branded OG share-card system + bio/blog discovery links; NCAA blog upgraded+indexed; recommendations reframed best->I-recommend; 4:5 IG card builder. App: exercise form-video library 4->29 (all oEmbed-verified), NEW Arm Care/Bands workout category + J-Bands routine, Speed section sharpened for running speed, Calendar prescriptions->guidance, QOTD repeat-bug fixed, recruiting-calendar->sport-specific NCSA, Gym->Workout rename. NEXT: monitoring (newsletter 6/23, Meta relaunch 6/24, NCAA IG post 6/24); revisit homepage conversion ~6/30 after ad data. PREVIOUS 2026-06-21 — **Conversion funnel rebuilt + signup fix SHIPPED.** 3 commits to `main` (SW v367->v369) + a 4th at eod (v370): front-door swap (homepage hero "free email" link + newsletter capture moved above Pricing + one-step inline email box on the high-school blog post + GA4 added to all 10 blog pages, previously ZERO analytics — `8a6a331`), the new `/links` bio hub (`833a63b`), and the signup plan-picker scroll fix so the CTA is reachable on short phones + "Free now. No charge until day 14." copy (`5166df5`). The #1 pre-6/24 task is DONE 3 days early. eod push: QOTD quote -> "A loss, a strikeout, or an error is not failure. It's information. We don't fail. We learn." (SW v370). Decisions: organic->newsletter / paid->app trial (app-signup-first leaked ~97% of cold traffic, 647->17); /links = evergreen bio hub; daily QOTD curated one-by-one. **NEXT: confirm Mon 6/22 posts fire + the Meta relaunch 6/24** (leave the ads as scheduled — the signup leak they hit is now fixed). PREVIOUS: refreshed 2026-06-19 — **Academics School-Year Auto-Reset + Quarter/Semester/Year/Cumulative GPA — SHIPPED in working tree** (app-only, `softball.html`, NOT yet committed/pushed): grading-period selector (Q1·Q2·Sem 1 / Q3·Q4·Sem 2) over the 6-class grid, quarters forecast the semester (the saved year-of-record), per-class Reg/Honors/AP → weighted (5.0) GPA beside the unweighted, dual **This Year + Cumulative** hero, **Academic History / transcript** card, and an **August auto-reset** (archive finishing year → clear slots → idempotent stamp; reuses `_mgSchoolYearEnd()`) with legacy migration. All FREE. Verified: syntax-clean + 39 unit tests (GPA + rollover) + live preview. NEXT: Coach reviews in-app → commit + SW bump. PREVIOUS: refreshed 2026-06-18 — **D1 "MyGrind Partner" Badge Live In-App + 6/16 Newsletter Confirmed Sent.** 1 commit, SW v358 — swapped the in-app D1 Training Resources tag from the neutral "Northridge · Ages 7+" placeholder to the live **"MyGrind Partner"** badge now that the partnership is formal (handshake 6/17, 250 regs granted); `softball.html` + SW bump. The 6/16 newsletter "The Most Important Month Has No Games" is CONFIRMED SENT. The in-app "MyGrind Partner" tag swap is now DONE (was a queued 6/17 item). **NEXT: close the Meta day-5 kill/scale read** — manual Ads Manager screenshot (Supermetrics auto-pull dead), Ads view Jun 12→today, gates CTR<0.7% & cost/LPV>$2.50 (6/14 baseline 4.02%/$0.28), confirm the ad set has a ~10-day END DATE. ALT build = the D1 "Official Partners" announcement, still BLOCKED on DJ's logo files (emailed 6/17, none yet). ——— refreshed 2026-06-17 — **D1 Partnership OFFICIAL + Proof-Forward Landing + Day-5 Read Opened.** 2 commits 6/17 (`65cb6c7` proof-forward hero leading with the Steven P. 5★ review + app-before-how-it-works; `fb82ba7` GA4 `trial_start_click` on the landing CTAs). **D1 Training Northridge partnership is FORMAL** (in-person handshake 6/17): 250 regs granted (matches the live D1GRIND cap), Coach at the July grand opening, national-chain "prove-it-locally" upside, multi-sport agreed path; now unblocked = swap the in-app Resources tag → "MyGrind Partner". **NEXT: close the Meta day-5 kill/scale read** — Supermetrics auto-pull is dead (team trial expired 5/15), so it's a manual Ads Manager screenshot (Ads view, Jun 12→today, columns CTR / Amount spent / cost-per-LPV / Results vs gates CTR<0.7% & cost/LPV>$2.50; 6/14 baseline 4.02%/$0.28); also confirm the ad set has a ~10-day END DATE. ALT build = the D1 "Official Partners" announcement once DJ emails his logos (`Business Docs/Logos/` staged). GA4 read flagged /signup.html 8s dwell as a possible funnel leak. ——— refreshed 2026-06-16 — **D1GRIND Launch Kit BUILT + LIVE + Testimonial on Landing.** 2 commits, SW v355. **D1GRIND wired end-to-end** (`99724ab` — D1 families get 6 months free; client PROMO_INFO + server FOUNDER_CODES + cohort-store caps, own cap 250) + **parent testimonial on the landing** (`f5c0532` — Steven P. 5★). Stripe promo `D1GRIND` minted live (coupon `bVat04tT`, 6mo/100%, max 250, first-time); link `signup.html?team=D1NORTHRIDGE&promo=D1GRIND` verified; print assets (flyer/one-pager/QR) in Business Docs. The D1 launch kit went QUEUED→BUILT+LIVE; DJ pulled the meeting forward to 6/17. ——— refreshed 2026-06-14 — **First Ad Verified Working + First Testimonial.** 0 commits (marketing/ops day, Coach 1 day post-dental-surgery). Confirmed the $100 Meta ad is beating both kill gates — **CTR 4.02%** (>0.7%) + **cost/LPV $0.28** (<$2.50), ~42 results / ~$11.76 spent, still in learning. Rejected Meta AI's budget-raise nudge (no mid-learning hikes); locked the 4.02%/$0.28 baseline into the 6/17 day-5 check-in (also corrected it to ONE live ad, not 3; softball+academics staged-not-uploaded). Captured the first written testimonial (Steven P., parent of sub #2 Aaron Pulley) → memory `testimonial-steven-pulley`; drafted a Google-review ask (PWA = no App Store). Started Instagram→Business Suite inbox connection. **NEXT (Mon 6/15): import the 6/16 newsletter to Mailchimp** (reminder fires 9am); alt = D1GRIND launch kit (Stripe first); do NOT touch the ad until the 6/17 read. ——— refreshed 2026-06-12 — **UX Bug Fixes + Landing Refresh + First Paid Ad Prepped.** 4 commits, SW v349→v353. **Fixed the worst real-user bug: bottom tabs going dead after rotating the phone** (`94dcc49`) — a landscape phone is ≥768px wide, crossing the desktop breakpoint and swapping the whole layout model on every rotation, which leaves iOS Safari's fixed nav with a stale touch hit-region (taps land on nothing until reload/sign-out); fix gates the desktop layout behind `and (pointer: fine)` so phones stay in one layout in both orientations + an orientationchange re-pin; same commit doubled the inactive-tab contrast (2.5:1→5.8:1) for sunlight. **Outdoor Mode** (`5ff96a1`) — optional Settings toggle (default OFF) that boosts contrast for the field without a light theme (overrides the muted palette vars + drops the diamond bg pattern; cloud-synced). **App chrome cleanup** (`d39c9de` — account bar slimmed, duplicate Settings card removed, 54 emojis → SVG sprites) + **mobile nav seam fix + all 6 landing screenshots rebuilt** and reordered by analytics (`8ea126d` — dashboard→game→academics→calendar→reflection→softball). **GO-TO-MARKET: first $100 Meta (FB+IG) ad fully built + staged** for Coach to publish ~6/13 — 3 ads (real dugout reel / academics / softball), 9:16 + 4:5, copy + Ads Manager checklist, UTMs `first100`/`video|academics|softball`, pixel `326334441984368` verified, day-5 read scheduled 6/17; LinkedIn rejected. Numbers: a new full-price $14.99/mo card-on-file trial (`cus_UgeTmWTgUJA5Af`, first charge ~6/25) = strongest willingness-to-pay signal yet. Standing rules saved to memory: `higgsfield-ad-creative-rules`, `meta-first100-campaign`. **NEXT: Coach publishes the Meta ad (~6/13, verify pixel in Events Manager); recommended build = the D1GRIND launch kit (carried, untouched today).** ——— refreshed 2026-06-11 — **Calendar Truth Day + D1 Partnership + Referral Loop.** 18 commits, SW v337→v349. **Calendar fact-checked vs the real world** (Pitch Smart pitcher shutdown in May-Jul, August tryout season) and EVERY surface now reads it live: This Month's Focus, Coach's Note (signs "Coach," not the retired ". The Grind"), month banner, Today's Plan (was one static rotation prescribing lifting in June's rest window — now phase-aware, verified 12x7), calendar phase badges (The Ramp / Rest + Recovery / The Build / Peak Strength / The Bridge), gym-form phase line. **Referral loop v1 live** (post-save "Text a Teammate" + `?team=` attribution → Firestore teamCode + firstEntryAt Day-0 stamps). **Gym Top Set PR tracking live** (dashboard Personal Records + NEW PR toast). **Smart back nav** (Resources/5 Tools dead ends fixed; mgBack returns to origin tab). **Blog #8 pillar** (training calendar month-by-month, FAQPage schema). **6/16 newsletter COMPLETE** (both affiliate links in; Mon 6/15 9am import reminder scheduled). BUSINESS: subs 1→2 (new $9.99/mo founder-discounted — identify `cus_UPJeF7tMwngcJf` in Firebase Auth); 6/09 newsletter VERIFIED SENT (32.8% open / 4.9% click / 135); Brandon self-distributing to the AllStars; **D1 TRAINING PARTNERSHIP OPENED** — DJ Milonas (818-231-9001) verbal yes + "love to make something work," Northridge facility pre-open recruiting Founding Members, one-pager + textable PDF delivered, D1 on the in-app Resources page (Partner label held until formal), follow up ~2026-06-25. **NEXT: D1 launch kit (fresh-morning money path)** — mint `D1GRIND` on coupon `bVat04tT` (own cap, NOT FOUNDERMYGRIND), wire `getPromoInfo()` + checkout eligibility, cut `signup.html?team=D1NORTHRIDGE&promo=D1GRIND`. Alt: phone live-tests (teammate prompt, Top Set PR, ← Back pills, June month row). **Coach to-dos:** identify the new subscriber; $1,000 Fidelity→business owner contribution (book as owner's equity, confirm with Sam); carried: CRON_SECRET to Notes, Canva downgrade, FOREVERYOUNG backfill, Mailchimp double opt-in, delete ybg-content skill. ——— refreshed 2026-06-09 — **App UX deep pass + Morning QOTD push LIVE.** 9 commits, SW v333→v337. Closed the 2026-06-09 UX-audit tail (welcome card, empty-state CTA, "More menu"/card copy fixes, coach-reply minimums); ran a **flow audit** — one-ask-per-load interrupt budget (review moved 3→7-day streak), Dashboard 16→11 cards, LIVE check-in card, Journal reorder; **QOTD grammar pass** (4 fixes, cards re-rendered); **bottom cut-off fix** (social footer was stranded outside the scroll container); **MORNING QOTD push LIVE** (`?mode=qotd` → "☀️ Today's Grind is ready", GitHub Actions @ 7:30am PT, verified on phone). THREE daily automations now run: 7:30am QOTD, 6pm reminder, Monday digest. NON-CODE: CRON_SECRET rotated again (in Vercel + GitHub Actions secret + needs Coach's Notes); Canva audit → downgrade Pro→free (~$216/yr saved); Vercel CLI linked on this Mac. **NEXT: DISTRIBUTION, not code** — (1) verify the Tue 2026-06-09 newsletter actually sent; (2) Brandon (bsonnier) outreach — DEPENDENCY: grant his lifetime comp via `redeem-lifetime` curl (needs his account email) + turn off his Stripe auto-renew, THEN send the personal text. Alt code option: per-user push times (Vercel Pro + hourly cron, `PUSH_CRON_MODE=hourly` already supported). **Coach to-dos:** save the new CRON_SECRET to Notes; downgrade Canva to free; FOREVERYOUNG2026 backfill for son + granddaughter (not urgent). ——— refreshed 2026-06-07 — **closed the 2026-06-05 audit's P2 tail** (`e5cc3c9`: pre-switch backup, in-app upgrade-401 fix, **FOREVERYOUNG2026 server entitlement + 10-cap** via new `api/redeem-lifetime.js`, `?reset=1` hygiene), shipped a **3-day-streak push opt-in prompt** (`049ca7b`), and **swapped branding site-wide to the new black/white/gold lockup** (`3cc41f6`: icons, light logos, marks, og-card; dark pages repointed to the dark lockup). 3 commits, SW v324→v327. **NON-CODE: daily Web Push is now LIVE** — flipped `PUSH_DRY_RUN=false` + redeployed, verified `?test=1` → `sent:1`. The entire 2026-06-05 audit is CLOSED. Decisions #66/#67/#68 locked. **NEXT: warm outreach** (still 1 paying customer — bsonnier referral + coach \"free for your team\"; needs bsonnier first name + player name + target coaches) OR the carried QOTD elevation. **Tue 2026-06-09 9am:** send the \"Bigger Field, Bigger Decisions\" newsletter (built). **DEPENDENCY (Coach):** get son + granddaughter's signup emails off their phones → backfill their FOREVERYOUNG2026 lifetime via `/api/redeem-lifetime` (not urgent). ——— refreshed 2026-06-06 PM — EVENING: **shipped premium-polish + the full engagement loop + Web Push**. Warm-unified the brand palette + tactile buttons; fixed the Edit-Profile blank-page bug + QOTD attribution → "MyGrind" (one word); shipped "Your Week" recap/streak + install card, AVG + OBP/SLG + weekly-volume trend charts, smart journal prompts, achievement badges, in-app nudge + .ics; built + **ACTIVATED** Web Push (Phases A/B/C — a real `?test=1` push landed on Coach's phone). 12 commits, SW v321→v322. **NEXT (the #1 morning task): flip `PUSH_DRY_RUN=false` in Vercel + redeploy** so the daily push cron actually sends (it currently only LOGS); while in the Vercel env, confirm `STRIPE_SECRET_KEY` holds the real Stripe key (CRON_SECRET was a Stripe live key earlier — rotated). Then: a well-timed in-app push opt-in prompt (adoption lever, fire at first entry / 3-day streak), then the P2 audit tail. Decision #34 updated (QOTD = "MyGrind" one word); reminder channel = push, not SMS. ——— 2026-06-06 — TODAY (AM): **closed the deferred high-risk signup-funnel cluster** (self-path + fallback-kid account creation via a shared `finishSignup()`, signin `?auto=1` email-bomb gate, card-picker non-bypassable + abandoned-checkout resumable) + **4 P2 audit items** (locked-player re-pricing, nav deep-links, per-minor streak, admin rate-limiting) + **fixed the email-capture leak** (app Mailchimp sync no-cors→JSONP + restored the homepage newsletter form) + **built the Jun 9 newsletter + its SEO blog post** ("After 12U", FAQPage schema) + a newsletter-signup CTA across all blog posts + the full MyGrind lockup in the newsletter header. 8 commits, SW v317→v321. NON-CODE: set up Higgsfield with the current black/white/gold logo set + produced the first MyGrind reel; scrubbed YBG from the content skills (ybg-content delete reverted on the app re-sync — needs the Settings → Skills UI). Decision #65 (newsletter header = full lockup, every issue). **NEXT: finish the P2 tail** — #194/#258 (shared-device sign-out purge + pre-switch backup, needs a two-account live test), #266 (founder cap, run /phase5-preflight), #210 (lifetime server entitlement); then the carried QOTD elevation. **DEPENDENCY (Coach):** check Mailchimp double opt-in (likely why emails "don't show up") + delete `ybg-content` in Claude Desktop → Settings → Skills. ——— 2026-06-05 — **ran a 50-agent app security/correctness audit (38 findings) and shipped 11 fixes** across 3 commits (`571a2af`/`4bc9c61`/`4b03372`, SW v314→v317) — closed an unauth repeatable free-trial hole, a gift-checkout undercharge, 3 P0 shared-device minor-data leaks, an unauth minor-data feedback endpoint, 2 cloud-sync data-loss bugs, an OTP lockout, and a COPPA skip-trial gap; report at `Business Docs/MyGrind-App-Audit-2026-06-05.md`. **NEXT: finish the audit's signup-funnel cluster** (self-path account dead-end + card-picker bypass + signin auto-fire) — high blast radius, do with live testing; reminder set 6-06 9am. Then the QOTD elevation (carried from Friday) + the P2 tail. ——— 2026-06-04 — **SMS went LIVE** (Twilio TFV approved, SMS_LIVE=true, last launch blocker cleared, `2be29e6`); recovered from a Mac cleanup that wiped Desktop-only files — **rebuilt the QOTD card pipeline** (qotd-story.html + scripts/build_qotd.py) + **the Daily Grind checklist** (grind.html at /grind) INTO the GitHub-backed repo (`3ab0ca6`), **fixed the moved-repo path** across 8 automation files, **consolidated the app's two quote arrays to one** (YBG_QOTD), and **elevated 20 sport-paired baseball/softball quote themes** with Coach (staged in docs/QOTD_NEW_QUOTES.md). **NEXT: finish QOTD** — batch 3 sport-specific quotes + dedupe → integrate into YBG_QOTD → pick the card design (2 options) → render Story/Feed/Square; then turn on iCloud Desktop sync. Decisions #63 + #64 locked. ——— 2026-06-03 PM — PM session shipped 4 more commits: signup resume fix (returning users were stranded on the cut Screen 1 from the AM trim; now resume at s2), review prompt → Google Business reviews + testimonial capture, brand-entity sameAs expanded to 7 + Crunchbase added (3-way "MyGrind" name-collision defense vs the poker + AI-coach apps). **NEXT: fire the warm outreach** — bsonnier referral + coach "free for your team" pitch (paste-ready in the Drive doc; needs bsonnier first name + player name + target coaches). AM — App polish marathon shipped (8 commits, SW v309→v314; interaction/press-feedback layer, landing premium pass, H/W cloud sync, shared-device identity fix, signup trimmed to 6 steps, Settings findable, academics redesign + finals-window fix + NCAA core-check un-gated to free, all 6 landing screenshots refreshed; Decision #62: core check free / multi-year NCAA core tracker = next Pro — full detail in LAUNCH +16 in STATUS.md). Earlier 2026-05-30 — The 5 Tools education layer shipped (`8f9bf95`), GSC fixed to the www property + 4 blog URLs request-indexed, CA FTB 2023 notice handled (30-day extension). Earlier 2026-05-29: landing conversion + SEO overhaul + blog launch — see Day +11 in STATUS.md. Landing rebuilt to industry-standard order, 6 real screenshots, hero typewriter removed, FAQ overhauled to 8 real parent questions, single pricing CTA, and /blog with 3 SEO articles all live in production.)

**🔧 RUNNER FIX STAGED (2026-08-19, written 8/22):** the scheduled-task failure is SOLVED and documented (STATUS 8/19 + memory `social-posting-automation`): dead cwd on 3 tasks (recreated), permission stalls in `--permission-mode default` (settings allowlist proven insufficient by a probe), zombie runs hitting the 3-slot cap (killed). **Remaining step is Coach's:** run `set-task-auto-mode.py` in Terminal (sets `permissionMode: "auto"` on the 7 recurring tasks), quit + reopen the Claude app, then verify the next 7:08 leaves a `media/social/` folder + commit with no `Not auto-approving` line in `main.log`. Then hand-post the already-built `2026-08-20-one-number-two-weeks` pair.

**DONE 2026-05-30 — Google Search Console:** Confirmed the real data lives in the **www property** (the non-www property shows empty/redirects — apex 308s to www; see memory `gsc_use_www_property`). Sitemap submitted + read (7 discovered); homepage/privacy/terms indexed; the **4 blog URLs were request-indexed**. **NEW TOP PRIORITY: draft the next blog post** ("When Should You Switch Travel Teams?", already in the newsletter pipeline; also queued: youth pitch counts by age, what college coaches actually look at) to feed non-branded search now that the blog is indexing.

**ALSO P0 — Twilio TFV #27140216 (RESUBMITTED 2026-06-01):** Huvi (human reviewer) confirmed the BRN/EIN is valid and asked us to resubmit. Done 6-01 via the console with a public Drive folder (CP 575 + stamped Articles + Cert of Status + SMS consent screenshot) in the Additional Information field; replied to Huvi. Awaiting her manual review. SMS stays `SMS_DRY_RUN=true`. **Do NOT resubmit again unless she asks.** 7-day status check scheduled (`twilio-tfv-status-check-jun8`, fires 6-8). *(Next code options: finish the GA4 conversion funnel — add `begin_checkout` + `purchase` events, the deferred half of the 6-01 GA4 ship, then mark `sign_up` as a Key Event once it fires live — or wire the daily check-in at `/checkin.html` to Firestore via the TODO hooks.)*

1. **Smoke-walk mygrindapp.com on phone in incognito** (~60 sec). BEFORE launching ads. Land on landing → tap Start Free Trial → run Screens 1-7 → plan picker → tap Start Trial → Stripe Checkout opens → abort. If anything looks visibly wrong, fix first.
2. **Mirror Firestore rules into Firebase Console** (~3 min). Paste contents of `firestore.rules` at https://console.firebase.google.com/project/my-grind-b8486/firestore/rules → Publish. Until done, client-side child-profile writes fail (Admin SDK still works server-side).
3. **Launch first ad creative on @youngsbaseball** (Thursday morning, 2026-05-28). Memory `mygrind_launch_funnel` says @mygrindapp has bot followers — paid promo routes through @youngsbaseball.
4. **Watch FOUNDERMYGRIND counter** as ads run. Token is now HEADER-only (not in the URL): `curl https://www.mygrindapp.com/api/admin/founder-count -H "Authorization: Bearer $ADMIN_RESCUE_TOKEN"` (token in Vercel env + Coach's Notes app). ⚠️ The old token was committed in this file's git history as a query param — ROTATE `ADMIN_RESCUE_TOKEN`. Currently 0/100. Alert in next_session_start when approaching 100 so Coach can flip to 3-month or 1-month promo per his locked plan.
5. **Monitor Vercel Analytics + Stripe Dashboard + Resend Dashboard** during first hour of impressions. Conversion math now has card-on-file at signup — expect 30-60% lower top-of-funnel but 5-10x higher trial-to-paid.
6. **Paste Part 2 newsletter into Mailchimp as draft** (~10 min, carried from Day +8). Full packet drafted 5-25. Subject "Here is what your coach sees. And why they are not saying it." Audience: MyGrind (`a7d801599a`).
7. **Paste Part 3 newsletter into Mailchimp as draft** (~10 min, carried). Subject "This is the part that tells you what to actually do." Send target Tue 6-2 9am PT.
8. ✅ **SHIPPED 2026-06-01** (`462f949`) — Replaced the in-app Pay-early Payment-Link path with the token-gated Stripe Customer Portal (`goToPurchase`/`goToCoachPurchase`/`goToTeamSponsorPurchase` in-app branch). Existing customers route to the Portal; only a genuine `no_customer` (404) falls through to a Payment Link; any other portal error fails closed. Parallel-subscription risk from the card-on-file ship is closed.
9. **Add LOCKED Decisions #52-#57 to Notion** (~10 min). Six decisions documented in 2026-05-27 session log need to be copied to canonical LOCKED Decisions page.
10. **Twilio TFV passive monitoring** — Huvi human-review escalation filed 5-22. Check inbox 1-2x/day. SMS stays in `SMS_DRY_RUN=true` until approval.
11. **Customer thank-you DM + Sam text + Notion expense row** for the $149.99 customer. Drafted in 5-25 session, still pending execution.
12. **Mailchimp `[ARCHIVED]` rename** of My Grind Weekly audience (~60 sec). Decision #51 locked. Just a settings edit.
13. **Mr. Sites LA Heat outreach** (carried). Suggested copy in `team_potential_collaborators` memory.
14. **Em-dash sweep continuation** (~370 instances repo-wide, task #14, deferred polish). Only landing + signup user-visible em-dashes done in pre-ad pass. Future pass must distinguish placeholder em-dashes (e.g. `<span id="x">—</span>`) from sentence em-dashes — file-by-file with quality checks.
15. **Daily morning affirmation feature** (task #27, ~3-4h, Coach said build later). Curated message library + 7am Resend email + in-app banner.

**NEW QUEUED SPEC (2026-06-03) — Multi-year NCAA core tracker (Pro):** the next Pro academic feature (Decision #62). Let a player log prior years (grades 9-12) → compute a true NCAA core-course GPA (16 core courses, sliding scale), show 16-course progress by category (English/Math/Science/etc.), per-target-school GPA gaps, and a junior-year "lock-in" alert. The free per-class safety check already warns when a class will not count; this is the prescriptive depth on top. Also serves a new user who wants to add prior years just to see where they stand. No "coming soon" UI in-app until it is built.

<!-- SHIPPED 2026-06-03 (App Polish Marathon + Academics Redesign + Findability + Founder Ops):
- 8 commits to main, SW v309->v314, all verified live.
- Interaction layer (40e223d, new assets/interactions.css): real pressed/:active feedback + eased motion + :focus-visible rings + reduced-motion across the app + all 7 core pages. Fixes "taps fire but feel stale" (mobile tap-highlight was off, almost no :active states). SW v310.
- Landing premium pass (348717c): elevated How-it-works cards, gold CTA + pricing depth, unified radii, scroll-reveal. + founder-note copy fix (index + foundermygrind): "parents I see every weekend" -> "players and parents I see every day".
- H/W growth log cloud sync (e8cd2bc): ybg_hw_log was the last unsynced key; push via fbSaveMisc + union-merge restore. SW v311.
- Shared-device identity fix (4437f4e): on a different-account sign-in, clear cached journal (canonical + all mm: minor namespaces) before cloud restore so Account A stops showing Account B on a family device; gated on a stored prior uid. SW v312.
- Signup trim (14e277b): cut Screen 1 re-explainer + pulled goals survey out of the funnel (now Step X of 6 via a STEP_ORDER list; goals data was unused).
- Settings findable (c2a5302): was buried under Develop; added a Profile-tab row + an account-bar gear. SW v313.
- Academics redesign (7ab43ba): GPA-hero + plain standing line on the dashboard (full NCAA grid stays in the Academics tab); finals-window lock fix (June prompts for final grades instead of falsely locking; Jul-Aug locks only once grades are in); un-gated the NCAA core-course safety check to FREE (coming-Aug-2026 Pro tease hidden). SW v314.
- Landing screenshots refreshed (f5af7db): all 6 regenerated headlessly at 1080x1440 to show the current 5-tab nav + HS academics (old set showed the retired 6-tab nav).
- Decision #62 locked: NCAA core-course safety check is free; the multi-year NCAA core tracker is the next Pro feature.
- Non-code: recovered + saved the 2 outreach drafts to Drive (Marketing & Social); built "The Daily Grind" daily founder checklist (~/Desktop/MyGrind Business/Daily Grind Checklist.html) + a recurring "Daily Grind" 8:00-8:20am calendar block.
- DEFERRED: multi-year tracker build (queued spec above), host the checklist at a private /grind URL for phone use, delete the old 8:00 "Start-of-Day Game Plan" event, dead s1/s4 signup markup cleanup.
- Smoke tests pending (Coach login): signup funnel end-to-end, two-account identity, H/W + check-in cross-device.
-->

<!-- SHIPPED 2026-06-02 (Newsletter Funnel + Social Rebrand + Conversion + Blog Engine):
- 9 commits to main (7 session + 2 pre-session morning), all verified live in production.
- Newsletter funnel: new /newsletter signup page (email-only, no app account), GA4 newsletter_signup event, self-propagating subscribe links in the Card Stack footer (c69d327); single-annual copy fixed $99 -> $99.99 to match Stripe.
- Check-in cloud sync (75883c0): checkin.html readiness + workload write to Firestore users/{uid} (managed-minor routing; doc-field storage so no rules change). Closes the 5-29 Firestore TODO. localStorage stays offline cache.
- Above-the-fold hero CTA (6c1d29c) on the landing -- closes the Day +11 "no hero CTA" follow-up.
- Phone optional at signup Screen 2 (7a9e30a) -- email alone creates the account; phone validated only if entered.
- SEO (528ed78): /newsletter added to sitemap + homepage footer link + lastmod bump.
- Blog +2: baseball-training-journal pillar (768a95e) + softball-training-journal (9f7437e). Both core keywords covered. Added to blog index + sitemap. Blog 4 -> 6 posts.
- Morning pre-session: GA4 checkout funnel begin_checkout + purchase + trial-conversion delivery (aa07315); nav redesign Option A 5-tab + Develop hub (16b30c9). These were the two queued "next code options" from the 6-01 ROADMAP -- now DONE.
- Non-code: all 4 social profiles rebranded to point at /newsletter (X = @MyGrindapp, kept 2022 age); Drive content bank (4 LinkedIn posts, 3 reel scripts, 2 outreach messages, 20 X posts + 10 reply-starters); Baseball Moms Uncensored comment; Metricool set up then shelved (manual posting); 2026-06-07 X-batch reminder set.
- Decision #61 locked: FOUNDERMYGRIND distributed case-by-case, not public. New memories: foundermygrind_case_by_case, mygrind_x_handle (@MyGrindapp), blog_content_roadmap.
- NEXT / DEFERRED: send the 2 drafted outreach messages (warm-client testimonial ask + coach "free for your team" pitch); blog post #3 (hitting slump, repurpose 6-02 newsletter); signup screen cuts (remove Screen 1 re-explainer + move Screen 4 goals survey to post-signup, renumbers flow, test in preview); landing testimonial block (needs warm-client quote).
- Smoke tests pending (need Coach login): check-in 2-device sync, signup with no phone.
-->

<!-- SHIPPED 2026-05-30 (The 5 Tools education layer + SEO/FTB cleanup):
- 1 commit 8f9bf95, live in prod, SW v296. New "The 5 Tools" panel under More (hit/power/run/arm/field + Pitching + position guide), joy/dream-first, sport-aware benchmark tables (HS Varsity/JuCo/College) shown per the Baseball/Softball toggle.
- Hit & Power reframed production-first (velocity tables removed). Run/Arm/Pitching keep numeric tables.
- 60-yard dash keeps a dated history with PR + faster/slower trend, sport-aware (softball = home-to-first), separate logs + per-metric validation; fills the Stats 60 field.
- Arm Health & Growth Plates card added to Resources (Pitch Smart for baseball, windmill note for softball). Resources split by sport (+PGF, +Alliance Fastpitch; Girls-in-Baseball baseball-only). Get Help moved to bottom. Hero clipping fixed.
- Decision #60 locked: 5 Tools = production-first + joy-first.
- Non-code: GSC www-property fix + 4 blog URLs request-indexed; morning-sync GSC link fixed; CA FTB 2023 Request-for-Tax-Return handled (30-day extension ~6-29 + weekly reminder ftb-2023-weekly-reminder); memories five_tools_benchmarks, gsc_use_www_property, irs_2023_taxes_june1.
-->

<!-- SHIPPED 2026-05-29 (PM — Security Audit + Auth Enforcement):
- Front-to-back security/perf audit on Opus 4.8. 5 commits to `main`, all deployed + verified live: 1995d10 6dca7f4 8ab3551 77944ce 57f8f94.
- Stripe webhook now returns 500 (not 200) on handler error so Stripe retries — was silently dropping a paying customer's onboarding; handler idempotency confirmed safe to retry.
- Admin rescue token (founder-count + signin-link) moved out of the URL query string into an Authorization / X-Admin-Token HEADER. Browser-URL workflow retired — use curl with the header.
- Security headers added in vercel.json: nosniff, X-Frame-Options SAMEORIGIN, Referrer-Policy, HSTS, Permissions-Policy + a Report-Only CSP (kept report-only by recommendation; nonce-based CSP is the future XSS win).
- CRITICAL closed: get-subscription / stripe-portal-session / feedback-list?parent= were unauth — anyone who knew an email could read billing state + a minor's coaching history, or open a billing portal. New lib/access.js requires a Firebase ID token whose email matches the requested email. Enforced by default (ACCESS_TOKEN_ENFORCE, kill switch=false). Verified live: anonymous -> 401, signed-in happy path confirmed by Coach.
- Perf: ~633 KB of base64 images externalized to content-hashed /assets/img/ + immutable cache (softball.html 1.5MB->1.17MB).
- Residual: feedback-list?player=<phone> stays open-but-PII-stripped until SMS verification (Twilio TFV) gives a phone<->identity link.
- ROTATE ADMIN_RESCUE_TOKEN — the old value was committed in this file's git history as a query param.
- New memory: security_audit_2026-05-29.md.
-->

<!-- SHIPPED 2026-05-29 (Day +11 — Landing Conversion + SEO Overhaul + Blog Launch):
- 11 commits 5-29 on top of the 5-28 Path B landing rebuild (7 commits). Full detail in STATUS.md Day +11.
- Landing rebuilt to industry-standard order: hero -> how it works -> see the app -> pricing -> FAQ -> founder note. Public landing split from /foundermygrind (noindexed) so cold ad traffic hits the standard trial funnel.
- Hero typewriter replaced with a fast staggered fade-in (settles ~1.2s vs ~11s). 6 real in-app screenshots in a uniform 3:4 grid. HOW IT WORKS reframed "more than a tracker" and moved above the screenshots.
- Bottom form-section + Mailchimp "Get Updates" email capture removed; founder note is now the single closing CTA. Pricing consolidated from 3 per-card buttons to one "Start My Free Trial" CTA.
- FAQ overhauled to 8 real parent questions (visible accordion + JSON-LD FAQPage kept in sync). Age range + founder personal name removed from all visible copy (name kept only in the hidden Person SEO node).
- SEO hygiene: sitemap lastmods refreshed, legacy-app + signin + coach-reply noindexed/disallowed. /blog launched: hub + 3 SEO articles (stats to track, NCAA eligibility GPA, training-journal habit) + shared blog.css + BlogPosting/Breadcrumb schema + sitemap entries + homepage footer link.
- All pushed + verified live. Commits: 34030fd 7f7813d a847a3c d919e90 515c338 5103bd2 0ac384b c0f292b 921007b 472e058 a9cc533.
- Open follow-ups: GSC reindex (now P0 above), dead form CSS cleanup, above-the-fold hero CTA (Coach deferred), app-wide name sweep of softball.html/signup.html.
-->

<!-- SHIPPED 2026-05-27 (Day +9 — Industry-Standard Parity Reached + Pre-Ad Polish):
- 24 commits to `main` — largest single-day ship since LAUNCH.
- **Code-based 6-digit sign-in replaces magic-link** (commits `2ef4c8c` + `31faa26` + `e7bb608`). New `/api/auth/request-code` + `/api/auth/verify-code` endpoints. Redis-hashed codes with 15-min TTL. Per-email + per-IP rate limits. signin.html UI rewritten to two-step email→code flow with auto-fire from signup Screen 8 ?auto=1 handoff. Magic-link verify endpoint kept alive so admin rescue endpoint at `/api/admin/signin-link?email=X&token=…` still works as backstop. SW v288 → v289. Industry-standard pattern — Slack/Notion/Linear all moved to code-based auth for the same iOS PWA Mail handoff reason.
- **COPPA exposure closed end-to-end:** Schema age range "9-22" → "13-22" in 3 places on index.html (commit `69938c6`). onboarding.html "Younger than 13" tile removed + softball.html age input min 5→13 + dead `isKid` prompt swap removed (commit `4d03112`). Privacy §5 vendor list expanded to name Firebase + Stripe + Resend + Mailchimp + Twilio + Vercel each linked (commit `86014c0`). Privacy §6.5 COPPA section with 5 subsections added (commit `335acdb`). `docs/COPPA-AUDIT.md` and `docs/COPPA-RUNBOOK.md` committed. SW v289 → v290.
- **Under-13 Parent-Managed Account flow LIVE** (commits `d1b79fb` + `5b4b6a2` + `ea6c570` + `740e858`). Stripe charge on Family plan serves as FTC-accepted verifiable parental consent — industry pattern from Khan Academy Kids / ClassDojo / Apple Family Sharing. New endpoints `/api/managed-minors-{create,list,delete}`. `lib/firebase-admin.js` gains `verifyBearer`, `assertFamilyPlanActive`, `getAdminFirestore` helpers. signup.html Screen 6 collects player age. Under-13 forces Family plan + shows COPPA banner. Defensive Mailchimp guard skips child emails. softball.html profile switcher bar for parents with managedMinors. Per-minor scope via `MgMinorScope` localStorage shim. SW v292.
- **Card-on-file at signup** (commit `369210a`) — Stripe Checkout `setup_intent` mode. Day-11 in-app banner + capture logic removed from softball.html. `mg_source` metadata renamed `signup_card_on_file`. Industry standard (Spotify/Netflix/Calm/ClassDojo). Privacy §3.1 + §4 + terms §5.1 + §5.4 + §5.7 + index.html founder copy updated for card-on-file language (commit `bb8f632`).
- **Firestore rules version-controlled** at `firestore.rules` (commit `c9c4e9a` + `740e858`). Mirror of live Console rules with new `managedMinors/{minorId}` + entries subcollection rules (parent-only). Header notes follow-up items.
- **Plan picker readability fix** (commit `46bbded`). `var(--gold)` etc rendered transparent because signup.html had never defined CSS variables at :root. Defined full MyGrind palette + hardcoded plan-picker hex values. BEST VALUE + RECOMMENDED pills + COPPA banner now visible. Card font sizes bumped.
- **QOTD audit fixed two coach-spotted factual errors**. "no batter on first" → "the runner on first never sees coming" (commit `50cc587` — batters bat, runners stand at first). "Rise ball...behind the mound" → "in the bullpen" (commit `0fb2e59` — softball has flat pitcher's circle, not a mound). "Conditioning shows up in the seventh inning" → "in the late innings" (same commit — sport-agnostic now works for baseball's 9-inning game).
- **First-signup default routing** (commit `2873083`). New users land on Profile (panel-players), tap "Continue to Academics" sets `mg_visited_academics` flag, every subsequent load defaults to Journal. Old `isProfileComplete`-only gate preserved as alternate graduation path. Per-profile scope: flag added to MgMinorScope SCOPED_KEYS so each child profile tracks its own onboarding state. SW v293.
- **More-section emojis swapped for Lucide icons** (commit `92636d7`). graduation-cap (Resources), bell (Daily Reminders), credit-card (Subscription), cloud (Save to Cloud — new `mg-icon-cloud` symbol added), mail (Send Feedback). requestNotifications success state rewrites button via innerHTML with SVGs instead of textContent emoji so the icon survives the 3-second feedback flash. SW v294.
- **Coach Feedback chips plain-English rewrite** (commit `bb36e63`). 8 labels updated across coach-reply.html + softball.html. data-key / data-cf-focus / data-cf-sit values preserved so SMS payloads + stored requests + downstream displays keep working.
- **Pre-ad em-dash sweep on landing + signup** (commit `efa74d5`). 8 user-visible em-dashes converted to commas/periods/colons. "— Now Live —" → "Now Live". "Straight up &mdash;" → "Straight up." etc.
- **Default journal entry Reflection → Practice + dead `applyAgeAwareJournalPrompts` fully removed** (commit `b971dab`). Function + 3 call sites deleted once verified the static HTML defaults match what the function had been restoring.
- **CRITICAL FIND mid-session:** `STRIPE_SECRET_KEY` env var in Vercel contained a publishable key value (`pk_live_…`) since project setup; never noticed because the only endpoint reading it (Day-11 banner via `create-checkout-session.js`) never fired in production. Coach pasted correct `sk_live_…` value, redeployed, smoke-tested working with `cs_live_…` returned. Stripe keys also scoped to Preview env target so feature-branch previews can run the full Stripe flow.
- **6 LOCKED Decisions:** #52 code sign-in replaces magic-link, #53 13+ posture across the app, #54 card-on-file at signup is the trial pattern, #55 under-13 supported via Parent-Managed Account flow on Family plan only, #56 always do the industry-standard pattern (meta-rule), #57 first-signup default routing Profile → Academics → Journal.
- **6 new memory files:** `feedback_always_industry_standard.md`, `coppa_under_13_parent_path.md`, `stripe_secret_key_misconfig.md`, `admin_rescue_endpoint.md`, `feedback_links_and_copy_blocks.md`, `advertising_launch_2026-05-28.md`. MEMORY.md index updated.
- **Production smoke tests at end of session 2026-05-27 PM:** all 5 green — `/api/create-checkout-session` returns `cs_live_…`, `/api/auth/request-code` returns ok, `/api/admin/signin-link` returns ok, privacy §6.5 renders, schema "ages 13-22".
- **App ready for paid + organic advertising launch 2026-05-28 morning.**
-->



<!-- SHIPPED 2026-05-25 (Day +7 — Memorial Day · Conversion Fix + First Real Customer + LLC Bank + Newsletter Voice Locked):
- Critical signup → magic-link conversion bug fixed end-to-end (commit `12b0f64`). Signup was creating localStorage state but never creating a Firebase Auth user and never sending a sign-in email. Explains the 56-person 5-20 broadcast producing 0 Auth users. Fix wires Screen 8: self-path → redirect to /signin.html?email=X&auto=1, parent-path → injected "ONE LAST STEP" banner. signin.html DOMContentLoaded handles ?auto=1 by auto-calling sendSignInLink(). Reuses existing branded-Resend + Firebase fallback chain. SW cache v287. Verified by Coach's stranger-phone walkthrough.
- First real revenue: $149.99 Family Annual subscription from inner-circle demo cohort (paid full price, not founder code). Net $144.68. Payout Jun 1.
- Stripe payout destination swapped: personal Chase ••9395 → Chase Business ••2761 (My Grind Sports LLC). Personal account removed. LLC commingling risk eliminated. Clean 1099-K reconciliation path locked for Sam.
- Twilio TFV escalation moved forward: Huvi (ticket #27140216) replied requesting docs, Coach sent CP 575 + Articles of Organization + Certificate of Status. Now in actual human review.
- 3 Decisions locked: #49 newsletter writing approach (hard-edged self-accountability), #50 newsletter must always integrate MyGrind with pain-point-specific scenarios, #51 Mailchimp dead audience stays (archive don't delete — cost-benefit math favors leaving the 18-month YBG-era campaign history intact).
- 2 new memory files: `newsletter_writing_approach.md` + `newsletter_app_integration.md`. MEMORY.md index updated.
- Part 2 newsletter rewritten 4x through harder voice + all-age framing + baseball-context-only + app-integration scenarios. Ready to paste tomorrow. Part 3 also prepped same treatment.
- Doc-sync commit `408ae00` cleared 5-24 STATUS/ROADMAP residual.
- Reflection: pivoted off the planned skill-building day as soon as the conversion bug was diagnosed. Right call. The fix unblocks every future conversion. Skills re-queued for Tue.
-->


<!-- SHIPPED 2026-05-23 (Day +5 — Resources Panel Refactor + Girls in Baseball + Stripe Tax Upload):
- Stripe 1099-K TIN switch unblocked via Dashboard → Settings → Tax Details document upload bypass. CP 575 located + uploaded mid-day. 24h review timer started. Means Phase 5 live-mode launch no longer gated on June 16 IRS database propagation.
- Resources panel rebuilt: moved out of Settings into dedicated `panel-resources` accessed via More → 🎓 Resources. 6 cards total. 19 actionable links + 5 Pro tips from Coach.
- Girls in Baseball section added — first audience-specific resource section in the app. WPBL launches August 2026, Baseball For All, USA Baseball Women's Team, MLB Develops, Hall of Fame Women in Baseball, LA Heat (SoCal regional partner). Intro reframed to growth narrative (WPBL launch + college club teams at Cal/UC Davis/USC/Washington/Occidental + NCAA Emerging Sport push).
- LA Heat partnership baked in — reciprocity for the Sites family who is already a MyGrind user since Apr 26.
- Email Coach option removed from in-app contact paths (Decision #47 — doesn't scale at national volume). Get Help section reframed FAQ-first.
- Readability bump on Resources panel: 20px Bebas Neue section heads, 11px → 13px badges, pro tip color/size fix.
- `.gitignore` hardened + `package-lock.json` committed.
- Firebase Analytics + journal_entry_created event wired in softball.html.
- Firebase Dynamic Links shutdown verified non-issue.
- Pronoun audit on user-facing copy: 0 changes needed.
- EIN propagation pattern documented as new project memory `[ein_propagation_pattern]`. EIN was issued 5-15 not 5-19 (memory previously conflated LLC approval date with EIN issuance date).
- 3 Decisions locked: #46 paid-trial pricing model holds through launch, #47 no direct email contact promoted in-app at national scale, #48 girls baseball is first-class audience.
- 6 commits: `8c4751d` `ece5ce8` `5a5a369` `5981a42` `a59dce8` `6feebce`. All Vercel READY. SW cache v277 → v286 across 8 bumps.
-->

### P1 — JSTV partnership follow-up (added 2026-05-22)
After tonight's lesson with daughter at Encino LL, evaluate the conversation outcome. If partnership intent confirmed: schedule weekend coffee with structured term sheet, draft mutual NDA, call Sam (818-988-8989) about equity vs profits interest vs rev share LLC implications.

<!-- SHIPPED 2026-05-22 (was Day +4 queue):
- Marketing Plan v0 drafted in Notion (https://www.notion.so/368819955e6f8170a1e8f499c8176e54) — 8 sections, recommended sport #3: soccer, 3 open questions for Coach.
- Stripe 1099-K TIN switch ticket filed via support.stripe.com.
- Twilio TFV escalation ticket filed via help.twilio.com with IRS CP 575 + Articles + Cert of Status attached. Root cause confirmed: EIN propagation lag.
- 7 code commits shipped (all 7 Vercel READY, 0 failures):
  - `fe37df5` + `c8f05ae` — Sign In header link on landing page (top-right, 15px/700)
  - `0d5e717` — Self-funnel signup copy gaps closed on Screens 5/7/8 + page title + meta description
  - `6827f23` — Coach Feedback MYFEEDBACK → MYCOACH reframing + honest SMS dry-run toast + Settings Recruiting & Academic Resources card (NCAA/NAIA/NJCAA/Perfect Game/PBR/Extra Inning Softball/NFCA/Khan/Common App/FAFSA/College Board)
  - `9aeafc1` — Dropped "Talk to Coach" group from Resources (Calendly was Coach-specific)
  - `62bb780` — Coach Feedback "didn't make sense" root cause fixed: removed all `data-default` canned phrases in coach-reply.html, added validation (chip detail ≥15 chars OR comment ≥30 chars), player-side dashboard now shows tap-to-text follow-up CTA
  - `7b9b130` — "Why I Grind" 200-char profile field + age-aware journal prompts (kid-friendly under 13)
- SW cache v272 → v276.
- Memory twilio_tfv_state.md rewritten with EIN-lag diagnosis.
-->

<!-- SHIPPED 2026-05-21 (was Day +3 queue):

<!-- SHIPPED 2026-05-21 (was Day +3 queue):
- Bank account opened — Chase Business Complete Checking #2909892761, Veterans Advantage tier, $0/mo. Done in-branch 2026-05-21 AM. Stripe payout destination update queued separately.
- FBN filed with LA County — confirmation #4179056, registers MyGrindApp + My Grind as DBAs. VitalChek email expected within 5 business days. Newspaper publication via Santa Clarita Gazette ($130) queued for after stamping.
- SMS-pending UX fix shipped (commit `b59dc50`) — Screen 7 + dashboard no longer falsely claim "Invite sent ✓" while Twilio TFV in carrier review. Honest "Setup link ready" copy + Share button. SMS_LIVE flag, flip to true on TFV approval.
- Signup gate wired into production Screen 0 (commit `2e59b26`) — payment chips, no-card pill, under-18 callout, device-path modal for parent funnel. Mockup design (commit `82da6cf`) preserved at /mockups/signup-gate.html.
- Multi-sport architecture LOCKED — Decisions #43 (one brand/site/app), #44 (national from day 1), #45 (sport advisor per new sport). Memory `multi_sport_strategy.md`.
- Stripe coupon state reconciled — FOUNDERMYGRIND verified ACTIVE (cap 100, 0 used). MYGRIND6 already INACTIVE. Memory `stripe_ids.md` updated.
- First wild customer feedback loop validated (Twin Dad) — bug diagnosed, patched, shipped, email reply sent via coach@mygrindapp.com all in same session.
- Day-5 VitalChek reminder routine scheduled for 2026-05-26 09:00 PT (trig_01EpYfPUaeTEtBamKHXDNX6s).
-->

### Multi-sport schema refactor (queued, 2026-05-21)
Gate work before sport #3 (~Nov 2026 target). Currently several baseball-specific stats / positions / coach-feedback strings are hardcoded in `softball.html` + `signup.html` + `onboarding.html`. Refactor to:
- Per-sport stat schema config (baseball OBP/AVG/SLG vs basketball FG%/AST/REB vs soccer goals/assists)
- Per-sport position list
- Per-sport coach feedback taxonomy
- Per-sport landing section template
Estimate: 2-3 focused sessions. Trigger when baseball/softball stabilize + 25+ paid signups land.

<!-- SHIPPED 2026-05-20:
- Launch broadcast email to full waitlist (~56) — "MyGrind is live" sent from coach@mygrindapp.com mid-morning. Done.
- Stripe business profile update — Public Business Name → My Grind Sports LLC, Customer Support legal name updated, Address Line 2 cleaned, phone verified. 90% done; the remaining 10% (1099-K TIN switch) requires Stripe Support ticket and is moved to today's #1 above.
- Twilio TFV Round 4 — filed inside 7-day prioritized window after Round 3 rejected for error 30527. Switched Registration Type BRN → EIN, value to 42-2579197.
- Mailchimp audience consolidation — merged to single audience (`73280b4c02e9bc56c7e633892`) and renamed.
- softball.html legacy github.io URLs purged (commit `2383fb8`).
- Profile input auto-format polish on blur (commit `561de18`).
- 5-channel launch social pack — IG Feed + Story + Facebook + LinkedIn + X posted; TikTok script ready, deferred to evening.
- Launch social posts Notion page created — https://www.notion.so/366819955e6f81f884b6c9831dcf2b8d (13 posts across 6 channels with checkboxes).
-->

### P0 — abfrianela email-mismatch bug (added 2026-05-20 PM, backlog)
**Symptom:** Settings shows different Firebase Auth email than localStorage profile (e.g. Coach's device showed `abfrianela@gmail.com` as Registered Email but his own Michael profile data in Journal). Happens when a different magic link is tapped on a device that already has another user's profile cached.

**Workaround for any affected customer:** Sign Out → Safari Settings → Clear Website Data for mygrindapp.com → sign back in with their actual email.

**Code fix (~30 min):** Detect Firebase Auth `currentUser.email` change vs cached `profile.email` mismatch on softball.html load. Either (a) reset localStorage on identity change, or (b) prompt user "you signed in as X but the journal is for Y — switch or merge?" Files: softball.html, around the cloud-sync prompt area.

### P0 — stale "14 days free, no card" share-caption copy (added 2026-05-20 PM, blocked on offer decision)
4 instances in `softball.html` SHARE_CAPTIONS + 4 in `mg-config.js`. The offer is now founder-rate (6 months free via FOUNDERMYGRIND) for the first 100, not a 14-day trial. Cannot sweep until Coach decides new copy: Option A "Try MyGrind free" (generic, true for all), Option B "Founder rate is open" (only true while 100-cap holds), or Option C explicit "6 months free for founders, then $99/yr". Decide first, sweep after. ~15 min after decision.

<!-- SHIPPED 2026-05-19 (was P0 above):
- Real-customer testing pass on launch-day fixes: COMPLETE. All 4 paths verified (FOREVERYOUNG2026, FOUNDERMYGRIND, default 14-day, onboarding-loop fix).
- Endpoint consolidation for branded email + Stripe portal: REPLACED with Vercel Pro upgrade ($20/mo). 12-function cap eliminated, hotfix `e65ea11` reverted in commit `5662ffe`. Both endpoints restored without consolidation. Branded email still gated on Resend domain verification.
-->

### P0 — Cross-cohort safety (added 2026-05-19 PM)
4. **`/api/admin/signin-link` admin rescue endpoint.** SHIPPED in commit `694e18c`; token transport hardened 2026-05-29 (header-only). Run: `curl "https://www.mygrindapp.com/api/admin/signin-link?email=CUSTOMER@EMAIL" -H "Authorization: Bearer $ADMIN_RESCUE_TOKEN"` → returns a working magic-link URL via Firebase Admin SDK; paste into iMessage to any stuck customer. Token in Vercel env + Coach's Notes app (browser-URL method no longer works). Use whenever a customer can't sign back in regardless of cause.
5. **Send Setup Link modal on signup.html Screen 8.** SHIPPED in commit `aeb4038`. Replaces dead Phase 3 placeholder alert with 4-path share modal (Copy Link, Send via Messages, Open on this device, QR Code). No backend dependency, no phone-number ask.
6. **SMS opt-in consent strengthened.** SHIPPED in commit `d0e1df1`. Added HELP keyword, message frequency disclosure, inline Privacy Policy link. Clears Twilio TFV Errors 30506/30513.

### P0 — Polish leftover from the audit
1. **Fix duplicate `escapeHtml()` in `signup.html`** — defined at lines 3264 + 3417. Consolidate to one definition. ~5 min.
2. **Fix the SMS preview URL in `signup.html`** — `buildSmsForPlayer()` (line ~3344) hardcodes `mygrind.app/start/abc123`. Should use `mygrindapp.com/onboarding.html?name=...` so the preview matches what Twilio will actually send when Phase 3b unblocks. ~10 min.

### P0 — Subscription / cancellation policy (added 2026-05-11)
3. **Stripe webhook → parent cancellation email.** When `customer.subscription.deleted` (or `customer.subscription.updated` with `cancel_at_period_end=true`) fires, send a confirmation email to the billing email on file via Resend. Body: "Your MyGrind subscription has been canceled. You keep access through {period_end_date}. Your player's journal will be held for 90 days — re-subscribe within that window to restore everything." Update `api/stripe-webhook.js` to handle these events; pull billing email from the Stripe `customer` object. The user-facing policy is already live in Settings (commit `717fa44`) — without this backend hookup, the parent-notification promise doesn't fire. ~1-2 hr.
4. **90-day retention purge job.** Scheduled Vercel cron (or Firebase Cloud Function) that runs daily, finds accounts canceled >90 days ago, and permanently deletes: Redis subscription record, Firebase `entries` / `goals` / `profile` collections for that uid, and any Resend audience entry. Add a `canceled_at` timestamp to the Redis subscription record at cancellation time so the purge job has a reliable signal. Surface "Account permanently deleted on {date}" email at the 90-day mark. The user-facing policy promises this in Settings — backend job has to honor it. ~3-4 hr including testing.

<!-- Phase 6 placeholder buttons fixed 2026-05-02 — Share + Settings now open real modals. -->

### P1 — Real-user walks (per CLAUDE.md verification protocol)
4. **Walk every screen of `signup.html` on phone width** — confirm progress dots, validation, back-nav, and SMS preview all behave end-to-end after recent edits.
5. **Walk every screen of `onboarding.html` on phone width** — verify the CSS-corruption fix from 2026-05-02 actually restored sticky progress bar + min-height layout. Especially S0 → S05 (PIN) → S1 → S14 path.
6. **Investigate "hover" issue** — user reported can't hover at top of something on Daily Journal. Need screenshot. Then fix. ~15 min.
7. **Investigate "softball image where baseball should be"** — need screenshot. ~15 min.
8. **Title fallback for Instructor entries** — currently saves as "Entry" if title is hidden. Should auto-fill from instructor name + focus. ~10 min.

---

## 📅 Next 2 Weeks

<!-- Phase 5 SHIPPED 2026-05-05. See "Recently Shipped" below. -->
<!-- Phase 6 SHIPPED 2026-05-02. See "Recently Shipped" below. -->

### P2 — Phase 5 follow-up: full account-edit Settings
Phase 5 backend is now in place. Upgrade the Settings modal from read-only to support inline email/phone updates and a Stripe billing-portal link. Currently the modal points users at support@mygrindapp.com for changes.

### P3 — DNS migration so the digest can email real parents
Move nameservers from Namecheap Web Hosting DNS to Cloudflare → verify `mygrindapp.com` in Resend → set `RESEND_FROM=MyGrind <notifications@mygrindapp.com>` in Vercel → delete `WEEKLY_DIGEST_TEST_EMAIL`. ~1 hr active + 24-48hr propagation. Risk: must capture all current records before changing nameservers, or website + `@mygrindapp.com` mail goes down. Current records to preserve (verified 2026-05-13): A → 216.198.79.1 (Vercel apex); MX → mx1.privateemail.com + mx2.privateemail.com (priority 10); TXT SPF → `v=spf1 +mx +a +ip4:192.64.118.103 +include:spf.web-hosting.com +include:spf.privateemail.com ~all`; TXT DKIM at `default._domainkey`; TXT DMARC at `_dmarc` → `v=DMARC1; p=none;`. When verifying with Resend, the SPF will need to grow to `include:_spf.resend.com` and a new Resend DKIM TXT record will need to land.

### P5 — Phase 7 (Player dashboard)
`onboarding.html` S14 currently shows a holding-screen overlay. Phase 7 is the real player dashboard — likely a stripped-down `softball.html` for the kid side or a new `player-dashboard.html`.

### P6 — Real-time Cloud Sync (Phase B v3) — DIAGNOSED 2026-05-31
Third attempt at auto-sync. The first two attempts caused full UI breakage in `softball.html`. **Do this only after P1 done AND with a clear test plan.** Key lesson: don't add a full-screen overlay that can become unclickable. Use a non-blocking pattern.

**2026-05-31 diagnosis (investigation done — next session can go straight to the fix):**
- **Push UP works for entries + profile.** `saveEntry()` (line ~9200) auto-calls `fbSaveEntry(latest)` reading the canonical `ybg_softball_state`; `saveProfile()` (line ~13466) auto-calls `fbSaveProfile` reading canonical `ybg_softball_profile`. Both push correctly to Firestore for signed-in users.
- **🐞 ROOT BUG — restore writes to the WRONG keys.** `syncFromCloud()` (line ~19467) writes restored profile to `ybg_profile` (line 19477) and restored entries to `ybg_state` (line 19503) — the **OLD** keys. The live app reads the **canonical** `ybg_softball_profile` / `ybg_softball_state`. There is NO migration bridging them (only `migrateSixtyLog`, unrelated). So cloud data restores into keys the app doesn't read → on a fresh device or after iOS storage eviction, the backup may not actually repopulate the app. Strong candidate for the prior "sync seemed broken" history.
- **Coverage gaps (push UP):** goals only push during the manual `bulkBackupToCloud()` (`fbSaveState` called only at ~19520 / ~19568, not on the goals-edit path); academics grades (`ybg_subject_grades`), settings (`ybg_settings`), homework log (`ybg_hw_log`) are never pushed. So even a signed-in user losing localStorage loses goals/academics/settings.
- **Biggest exposure: local-only (not-signed-in) users sync NOTHING** — all `fbSave*` are guarded by `if (!fbUser) return`. Their data is localStorage-only → fully exposed to iOS Safari's ~7-day storage eviction.
- **Safe fix plan (incremental, testable, do NOT rewrite restore-merge blind):**
  1. **Push-UP coverage (LOW risk, additive, fire-and-forget):** wire `fbSaveState`/new `fbSaveMisc` into the goals + academics + settings save paths, guarded by `if (!fbUser) return`. Can't corrupt local data or break UI — only writes more to cloud.
  2. **Fix the restore key mismatch (MEDIUM risk — needs device test):** point `syncFromCloud` at the canonical keys, preserving the existing local-wins merge (`Object.assign({}, cloud, local)`) so we never clobber fresher local data. Test two-device: sign in on A, create data, sign in on B, confirm it appears; then clear A's storage, re-sign-in, confirm restore.
  3. **Reduce local-only exposure:** make the cloud-backup nudge harder to ignore / encourage sign-in (don't force — COPPA local-only paths exist).
  - **Verify on a real device before shipping** (per CLAUDE.md). This is a source-of-truth change; rushing it is how the UI broke twice.

### P7 — Discoverability ("Why isn't anyone using this?")
If beta users aren't logging entries daily:
- First-run tour (3 quick screens explaining the app)
- Simpler dashboard for new users ("start here" card)
- Clearer call-to-action on the trial signup

### 🎯 Navigation redesign — Option A (DECIDED 2026-05-31, recommended next build)
Coach reviewed the flow and picked Option A: the nav had **6 bottom tabs + a "More" drawer = 13 destinations**, tuned by feel with real churn (Stats moved in/out, Academics promoted, Calendar cut). Consolidate to **5 clean tabs, no More menu.**

**Target structure:**
1. **🏠 Home** (Dashboard) — also surface **Daily Check-In** (`checkin.html`) as a card here (daily habit, currently buried in More).
2. **📓 Journal** — log + history (the core daily action).
3. **📊 Stats** — performance numbers.
4. **🎯 Develop** (NEW hub panel) — a card menu linking to **The 5 Tools** (`panel-tools`), **Resources** (`panel-resources`), **12-Month Calendar**, **Goals** (`panel-goals`), and **Academics** (`panel-academics`, folded in from its standalone tab). Doubles as the **MyGrind Pro upsell anchor** (free = reference layer, Pro = prescriptive academics + AI coach).
5. **👤 Profile** (`panel-players`) — player profile + Subscription + **Settings** (`panel-settings`) + Help/FAQ + Send Feedback. (Sign Out is already surfaced in the account bar as of v305.)

**Build increments (syntax-gate + deploy each, verify on a real phone):**
1. Build `panel-develop` (new hub with nav cards → tools / resources / calendar / goals / academics).
2. Rewire `#mg-bottom-nav` 6→5: Dashboard(→Home), Journal, Stats, Develop, Profile. Remove the Academics + More buttons; update `mgTab()` ids + active-state highlight.
3. Move More-menu utility (Settings, Help/FAQ, Send Feedback, Suggest a Feature) into Profile; retire `#mg-more-menu` + `toggleMoreMenu()`.
4. Surface the Daily Check-In card on Dashboard.

⚠️ Nav is central (many `mgTab()` callsites + the More menu). Test every tab + every Develop card on a phone before calling it done. Also dedupe the pre-existing **duplicate `id="panel-resources"`** (lines ~6124 + ~6451) during this work. This change supersedes Decision #(Academics-own-tab); Academics now lives inside Develop.

---

## 🌅 Future / Maybe

### 📧 Newsletter web archive (`/newsletter`) — queued 2026-05-31, recommended
Build a web archive of MyGrind Weekly issues at `mygrindapp.com/newsletter`, mirroring the `/blog` structure (flat HTML per issue + an index page, blog.css, BlogPosting/CollectionPage JSON-LD, sitemap entries). Each sent issue becomes its own indexable page. **Why now:** (1) social posts + emails need a real shareable URL per issue (you can't deep-link a sent Mailchimp email); (2) compounds the blog SEO play — every issue is another search-landing page; (3) lets players/parents browse and pick past issues (Coach's "let them choose what to read" goal) without cluttering the journal app. **Flow:** short teaser email -> "read this week's issue" web link -> archive page that cross-links the blog + a soft signup CTA. The newsletter and blog become one content hub, two front doors (email + search). Source: the Card Stack newsletter build, 2026-05-31. Also wire the long-deferred Dashboard card "📧 Read this week's issue" -> latest archive URL. Convert the Card Stack template body into the archive page layout (drop the email-table wrapper, use blog.css).


- Push notifications (real lock-screen pushes — requires Firebase Cloud Functions, $25/mo)
- **Native app via Capacitor wrapper (NOT raw WKWebView) — Future, traction-gated** — Added 2026-05-31 strategic conversation. Decision: when we go native, use **Capacitor** (loads our existing web app in a native shell + native plugins), never a raw WKWebView wrapper (Apple rejects under guideline 4.2). **Two modes, and the choice drives how much prep matters:** (a) **remote-URL mode** (`server.url = https://www.mygrindapp.com/softball.html`) — native shell loads the live site, we keep instant web deploys, near-zero code change, but requires network (no offline) and slightly more App Store scrutiny; (b) **bundled mode** — assets ship inside the app for offline, needs the prep work below. **Why eventually switch:** lower install friction than the clunky iOS "Add to Home Screen" flow, reliable native push (retention lever for streaks/reminders), durable storage (sidesteps the iOS 7-day storage-eviction risk), App Store discovery + parent trust. Also resolves the Brandon iOS Home-Screen sign-in handoff bug (see Bugs section). **IAP note:** post-April-2025 Epic ruling, US apps can link out to external Stripe checkout without Apple's commission (external-link entitlement) — verify current state when building; keep billing on the web/external, don't deep-couple in-app web checkout. **PREP NOW (low-regret, helps the PWA too, build nothing we'd rip out):** (1) **Cloud sync = durable source of truth** — elevate P6; 64 `localStorage.setItem` calls vs partial Firestore today means a lapsed iOS PWA user can lose journal data NOW. This is the #1 item and protects current users regardless of native. Abstract storage behind one read/write layer so localStorage→Capacitor Preferences/SQLite is a one-file swap. (2) **`API_BASE` constant** — 18 relative `fetch('/api...')` calls work in PWA + remote-URL mode but break if bundled; centralize into one base var. (3) **`mgNotify()` helper** — wrap the 2 web `Notification` calls so native push is a one-function swap. (4) **`mgShare()`/external-open helper** — centralize the 23 `navigator.share` + 21 `window.open` + mailto/sms calls for the Capacitor Share/Browser plugins. (5) **Keep auth code-based only** in the app path (request-code/verify-code already primary + works in webview); do NOT expand the legacy `sendSignInLinkToEmail` magic-link (it's the Brandon bug, breaks the same way in a webview). **DO NOT build now (would be rework):** elaborate web-push/service-worker-push infra (do push native via Capacitor instead), heavy SW offline-caching logic (native handles offline differently), or anything assuming in-app web checkout is permanent. **Trigger to actually build:** parents asking for an App Store download, push reminders become a measured retention need, or install-friction is measurably costing activation. Tool when we go: Capacitor + Push Notifications + Preferences/Filesystem + Share/Browser plugins.
- Multi-player accounts (one parent buys, multiple kids use)
- **Coach-facing section / dashboard — Phase 7b V2 (MyGrind Pro, August 2026 target)** — Coach Young 2026-05-16 walkthrough decision: launch the player app first, build the coach side once schools restart in August. Per Decision #27, the coach dashboard is the paid Pro tier (free tier keeps the 1-to-1 SMS feedback loop from Phase 7b V1). Scope: a coach-facing area inside softball.html (or a separate coach.html) where a coach signed in with their email sees a roster of all their players, recent entries summary, ability to send/receive feedback at scale (not 1-by-1 SMS), and team-level stats dashboard. Phase 7b V1 (player → coach SMS via coach-reply.html) already shipped 2026-05-04 — Phase 7b V2 is the Pro-tier dashboard layer on top. Lucide icon swap (above) should land BEFORE this work so the coach view uses the polished icon system from day one.
- **Team management section (August 2026 target)** — Coach Young 2026-05-16 walkthrough decision: hidden from More menu for launch, panel still in DOM (`#panel-team` in softball.html around line 4088). Currently shows a "join a team or create one as a coach" hero card but the join/create/invite flow isn't fully wired to a backend. Re-enable the My Team button in the More menu (currently commented out around line 2025) once the flow is complete and the coach-facing roster view exists. Belongs in the same August sprint as the coach-facing section above.
- **Parent invite — alternative paths at signup (post-launch P1)** — Coach Young 2026-05-14 walkthrough. Current signup assumes the player has a phone for the SMS invite. Many 8-12 year olds do not. Add two alternative paths on Screen 6: (a) "I'll set up MyGrind on this device right now" — skips SMS entirely and routes the parent's device straight to onboarding (best UX for the parent-and-kid-in-same-room case); (b) "Send the link to my email instead" — Resend-backed email invite the parent opens on whatever device the player will use. Option (a) is ~1-2 hr (UI toggle + route handling). Option (b) is ~3-4 hr (UI + Resend backend + email template). Ship (a) first, (b) later if real users ask.
- **Parent weekly digest — opt-out mechanism (post-launch P1)** — Coach Young 2026-05-14 walkthrough. Current architecture: `api/cron/weekly-digest.js` fires every Monday and auto-sends to every parent email with feedback activity in the past 7 days. Player can also manually share anytime via the Share With Family card. What is missing: a parent-side opt-out. Add a One-click "Unsubscribe from weekly updates" link in every digest email that hits `/api/unsubscribe?token=...`, sets a Redis flag `feedback:digest:optout:<email>`, and serves a simple confirmation page. Update the cron loop to skip emails whose flag is set. ~1-2 hr. CAN-SPAM compliance also benefits from this. The digest can stay opt-out (auto-enrolled) per Coach's intent.
- **Photo + video on entries (MyGrind Pro)** — Firebase Storage backed, cross-device, accessible to coaches inside the Pro coach portal. Live-app UI pulled 2026-05-11 because localStorage stripped media dataUrls on save (5MB cap) and photos vanished on reload. JS hooks left dormant in `softball.html` (handleFileSelect / processFiles / pendingMedia / renderMediaPreview) — wire to new affordance + Firebase Storage upload when Pro lands.
- **My Photobook (MyGrind Pro)** — dashboard entry button removed 2026-05-14 walkthrough. `openPhotobook()` aggregates every photo across all entries into a single grid view, but suffers the same localStorage-strip problem as inline photos above. Re-surface the entry button when the cloud-backed media system in MyGrind Pro lands (Phase 7b V2). Function + `#photobook-overlay` modal are still in code; just no UI entry point.
- **App-wide icon system — Lucide SVGs replace system emojis** — Coach Young 2026-05-14 walkthrough. The current app uses 30+ different system emojis as UI chrome (🏋️ 🤸 📋 📓 📚 💪 📷 🏆 etc.). Cross-platform rendering is inconsistent (iOS color, Android flatter, browser fallbacks), sizes drift across surfaces, and the colorful cartoon language clashes with the dark + gold premium brand. Top sports/training apps (Strava, Whoop, Nike Training Club, Apple Fitness+, Hudl, Linear, Notion) all use custom monochrome SVG icons in their brand color. Plan: add Lucide SVG sprite/helper, build `mgIcon(name, size)` function, replace utility emojis surface-by-surface starting with Dashboard. Keep emojis only where they carry cultural meaning: ⚾/🥎 (sport identifier), 🔥/💎/🧊 (streak tiers), 🦃/🎄 (seasonal). Scope: ~4-5 hr focused session. Best done with phone-side-by-side visual review.
- **Calendar — nutrition / recovery / busy-player tips per month** — Coach Young 2026-05-14 walkthrough. The 12-month calendar (`months[]` array around line 5227 in softball.html) already has `name`, `type`, `focus`, `intro`, `weeks`, and `note` per month, all in Coach's voice. Add three new fields: `nutrition` (eating like an athlete, month-specific — e.g. off-season needs more fuel for heavy lifting, in-season needs faster-recovery foods), `recovery` (sleep / mobility / arm care emphasis for the month), and `busyTips` (the journal version of "I had no time to log today — what's the 30-second version?"). Render in the expanded month card next to the weeks content. Voice has to be Coach's, not generic athlete-nutrition copy. Approach: draft structure first, write Jan + Jun together as samples, then batch the other 10 with Coach reviewing. ~30-45 min focused session.
- **Strength progress tracking for workouts (MyGrind Pro)** — per-exercise start weight + current max + pounds increased over time. Each workout entry's lifts get parsed (e.g. "Trap Bar DL 3x5 @ 225") so the app surfaces a strength-progression card showing PR history, plateau alerts, and "+X lbs since you started" callouts. Belongs in Pro because it needs cross-session aggregation, charts, and a structured lift-log schema (vs. today's freeform textarea). Coach Young noted 2026-05-11 during the workout-entry walkthrough.

- **"Ask Coach" AI chatbot (MyGrind Pro)** — Coach Young 2026-05-16 strategic conversation: AI trained on Coach Mike's 35 years of player development knowledge + the player's own journal data, providing personalized training advice. The player asks a question ("my swing is rolling over on outside pitches, what should I work on?") and the AI responds in Coach's voice, referencing the player's recent entries and goals. Premium MyGrind Pro feature, NOT a help-navigation bot — different product, different conversation. Backend: Vercel serverless function calling Claude Haiku or GPT-4o-mini API (~$0.002/query, ~$30-300/mo at 100-1000 daily users). Free tier keeps the existing static FAQ + mailto channels. Build trigger: only after Pro tier has paying users — economics don't work pre-revenue. Could anchor the Pro upgrade pitch entirely on its own ("Pro unlocks Ask Coach"). Source conversation captured in eod-sync log 2026-05-16.
- **Academics — Summer Mode (free tier, August 2026 target)** — Coach Young 2026-05-16 walkthrough surfaced the gap: current academics card assumes school is in session year-round and goes stale in June/July when no new grades arrive. Phase 2A (free-tier summer mode) transforms the Dashboard academics card in June-August: (1) **freeze the prior semester GPA + eligibility badge** in a "📚 Spring [Year] Final Grades · Locked" treatment (read-only, prevents stale-data drift); (2) **optional summer course tracker** — small input form that only appears if player taps "I'm taking summer classes", lets them log course + grade as they go; (3) **AP/honors prep checklist** for fall course readiness; (4) **ACT/SAT date countdown** for sophomores+; (5) **junior-year NCAA core-grade lock countdown** for upperclassmen; (6) **calendar academic tips swap to summer-coded copy** (reading, online courses, college prep) for June/July/August months. Auto-detect "is it summer" by current date (June-August = summer mode). When August arrives, card unlocks for fresh school year input. Build target: August sprint alongside coach-facing dashboard. ~2-3 hr focused work. Brand voice rules apply — no em-dashes, sport-neutral copy, Coach's direct voice.

- **Academics — NCAA Core-Course Tracker + advanced GPA (MyGrind Pro)** — Phase 1 (basic 6-subject view + GPA-based eligibility badges on Dashboard + Profile) shipped 2026-05-11. Phase 2A (summer mode) lands free tier in August 2026 per entry above. Phase 2B is the deep NCAA stack, locked to Pro per Coach Young: (1) full 16-core-course tracker across 4 HS years (4 yr English / 3 yr Math / 2 yr Science / 2 yr Social Studies / 4 yr additional / 1 extra Eng-Math-Sci), with year-by-year completion checkboxes; (2) NCAA-accurate core-course GPA calculation (different from school GPA — only specific classes count, scaled with honors/AP weighting); (3) junior-year lock-in alert (NCAA locks 7 of 16 core grades after junior year — Pro pings the player + parent before the deadline); (4) **dedicated Academics tab** (Coach Young 2026-05-11) — elevate from card-on-Dashboard to a top-level tab when Phase 2B features land. Card stays for the free-tier journal flow, tab unlocks under Pro for the depth; (5) **school-year-aware framework** (Coach Young 2026-05-16) — player enters their school start/end dates in Profile, app auto-detects school-year vs summer, GPA carries forward between years, Profile gets an Academic History view of past years. Source: NCAA Eligibility Center fact sheet + NCSA core-course guide.
- **Quote pool athletic-truthiness audit (post-launch P1)** — Coach Young 2026-05-16. Decision #42. 138 quotes across YBG_QUOTES + YBG_QOTD in softball.html (~line 12549 and ~line 16314). Triggered by spotting 'Your jersey number changes. Your work ethic doesn't.' on Dashboard — logically backwards because work ethic IS the thing a player controls. Read every quote against: (1) athletically true (not just inspirational fluff), (2) age-appropriate for baseball/softball players 11-18, (3) passes no-em-dash + sign-as-Coach-not-Coach-Young rules (Decisions #34 + #35), (4) reinforces something the player actually controls (locus of control test). Pull or rewrite anything that fails. ~60-90 min focused session. The 5-16 cleanup pulled 1 quote (pool 140→138); the remaining 138 still need a pass.\n\n- GameChanger import via backend proxy — deferred to post-launch upgrade tier (UI removed 2026-05-08, dead JS removed 2026-05-09; Option 1 was disabled because Anthropic API is blocked by browser CORS, would need a server-side endpoint)
- MaxPreps integration — deferred to post-launch upgrade tier (UI removed 2026-05-09; revisit once initial app is running and API partnership lands)
- Affirmation push notifications via SMS (Twilio integration)
- Internationalization (Spanish version for the Latino baseball community)

---

## 🐛 Bugs / User Feedback

*When a beta user reports something, jot it here with date and source.*

- **iOS PWA sign-in routing breaks Home Screen handoff** (reported 2026-05-26 by Brandon, beta user). When MyGrind is installed as a PWA on iOS Home Screen, tapping the magic-link from Mail opens Safari (not the PWA). User completes sign-in in Safari but the session lives in Safari's storage, so re-opening the Home Screen PWA shows them still signed out. Known iOS limitation — Apple does not let PWAs receive deep links from other apps. Brandon's workaround: stay on the web-based version. **Not blocking** (web version is primary product). **Defer to ~30+ founder cohort or post-multi-sport milestone.** Fix paths: (a) UX-level "return to Home Screen app" guidance after sign-in, (b) shared storage probe + auto-redirect, (c) native iOS app (months out). See conversation 2026-05-26.

---

## ✅ Recently Shipped (last 7 days)

*Move shipped items here from "This Week" so you have a record of progress.*

### 2026-05-18 (LAUNCH DAY · 32 commits · Cross-Device Sign-In Live)
- ✅ **LANDING FLIPPED TO LIVE** 10:49am PT — `index.html` waitlist form replaced with "Now Live" + primary CTA `/signup.html?promo=FOUNDERMYGRIND` (commit `7b8a1cb`).
- ✅ **4 customer-facing launch bugs found via real testing + fixed within hours**: onboarding→journal canonical key (`e42d220`); trial 7→14 days swept across 13 surfaces (`eed3e36`); defensive trial-grant in journal as safety net (`7dd8e88`); FOUNDERMYGRIND + FOREVERYOUNG2026 grant real access not just copy (`6cb9ae4`).
- ✅ **Cross-Device Sign-In V1 COMPLETE end-to-end** — new `signin.html` passwordless email-link flow (`582c2ac`). Firebase Console configured (Email/Password + Email link passwordless enabled, Authorized domains `mygrindapp.com` + `www.mygrindapp.com` added). Coach verified full round-trip on production. Entry points in signup Screen 0, softball welcome, landing footer, trial-abuse overlay (`64cdb36`), Settings Sign Out card (`a4f7481`).
- ✅ **Cloud backup banner + Settings card** — `bulkBackupToCloud()` uploads existing local journal entries to Firestore on first sign-in (`36a7ae5`, `52ce4a5`).
- ✅ **Trial-abuse Tier 1 LIVE** — atomic email + phone Redis SET NX block on re-signup (`043abec`).
- ✅ **Stripe webhook cancellation email** — closes Settings policy promise (`520668b`).
- ✅ **Option A v1 — Day-11 CC capture** — new `api/create-checkout-session.js` creates Stripe Checkout with `trial_end` set to MyGrind cliff. All behind `OPTION_A_DEFAULT_ENABLED = false` flag. Test plan in commit `8d793aa` message.
- ✅ **Install card permanent in Settings + iOS banner copy fix** — "Scroll down" → "Swipe up in the menu that opens" (`54a1c26`).
- ✅ **Sign Out + Screen 0 cross-device guardrail** (`a4f7481`).
- ✅ **Security audit follow-ups** — feedback endpoints rate-limited, PII hashed in Vercel logs (`36bd2fa`, `dfb2f46`).
- ✅ **Legal entity flip** — "My Grind Sports LLC" language live across privacy/terms/footer/schema, pre-CA-approval with rollback ready (`cd8e17b`).
- ✅ **Football multi-sport architecture** — added then hidden from public funnel for phased Phase 3 rollout (`8f6627b`, `aa20c5f`).
- ✅ **Em-dash sweep (Decision #35) fully closed** — 820 → 663, all remaining instances are internal-only (`48c5d74`, `c3ff11a`, `51f07fe`, `ff8770f`, `12fc62e`).
- 🛑 **Branded magic-link email + one-click Stripe Customer Portal** built then rolled back same night — Vercel Hobby plan caps at 12 serverless functions per deployment, my commits pushed to 14 and build failed. Hotfix `e65ea11` deleted `api/magic-link-request.js`, `api/magic-link-verify.js`, `api/stripe-portal-session.js`, `lib/firebase-admin.js`, and the `firebase-admin` dep. All callers have built-in fallbacks (signin.html falls to Firebase built-in email; Settings portal button falls to hardcoded Stripe login URL; signup dashboard portal link falls to mailto). Recovery: merge magic-link-request + magic-link-verify into single endpoint with `?action=request|verify`, merge stripe-portal-session into get-subscription — both polish features come back AND leaves 1 slot headroom. OR upgrade Vercel to Pro ($20/mo) to remove the cap.

### 2026-05-13 (Founder Offer + Email Stack — 3 commits + heavy ops work)
- Stripe coupon swap: `MYGRIND6` deactivated, new `FOUNDERMYGRIND` promotion code live (0/100, 6 months free). Coupon renamed "MyGrind Founders - 6 Month Free."
- `signup.html` — PROMO_INFO entry swapped, Screen 5 confirmation copy now reads "Founder code applied — your first 6 months are free. Welcome to the first 100." Added `?promo=<code>` URL param handler so launch-day email links land users on Screen 5 with the code already applied and the trial copy already flipped (commits `d229215`, `08eb626`).
- `index.html` — reframed as a founder offer. New `.founder-note` block between hero and form with a personal letter from Coach pitching FOUNDERMYGRIND. CTA copy swaps: "Notify Me" → "Reserve My Spot", "Be First To Know" → "Reserve Your 6 Months Free", trust line + success message rewritten in Coach's voice. Animation extended to fade in the founder note at 500ms (commit `a8fa3bb`).
- Email deliverability — SPF installed via cPanel suggested record, DMARC auto-validated, DKIM + PTR were already valid. All four green.
- Email routing untangled — MX has been pointing to Private Email all along (mx1/mx2.privateemail.com), not jellyfish.systems as old memory claimed. cPanel `coach@`/`support@` mailboxes were dead-letter boxes (could send, couldn't receive); deleted. Private Email confirmed as single source of truth. `coach@` Auto-Forward → Gmail configured.
- Decisions #31, #32, #33 locked (founder-letter voice for pre-launch, Stripe enforces the cap not the page/email, Private Email is sole inbound mailbox system).

### 2026-05-12 (Journal polish + Dashboard reorg + age band audit — 21 commits, SW v145 → v167)
- ✅ **Off Day flow built end-to-end** — 😴 dropdown label "(full rest)", blue info banner, lock system (when an Off Day exists, training types are disabled; only Reflection allowed), hard duplicate guard inside saveEntry (8 duplicate Off Days seen in walkthrough was the trigger), instant unlock on delete wired into renderAll, recursion-safe applyOffDayLock with _applyingOffDayLock flag. Commits `c3775b6`, `a793477`, `723da84`, `a51d7d5`, `8cfe1bb`, `d841895`, `b06d6ec`.
- ✅ **Off Day prompts rewritten — affirmation, recovery, family** — three distinct boxes that don't overlap with Reflection: "Today's affirmation" (seeded examples like "I am the player my team needs"), "How are you taking care of yourself?" (sleep/food/hydration/stretching/ice), "Who or what filled your cup today?" (family/friends/faith/fun). Reflection is addition not replacement — player can save Off Day then add Reflection on same date. Commit `b06d6ec`.
- ✅ **Reflection redesigned (inspired by Day One / Stoic / Reflectly)** — 🪞 intro card with italic invitation copy, mindset emoji picker (🔥 Fired up · 😌 Calm · 🙏 Grateful · 😐 Neutral · 😤 Frustrated · 😞 Down — saved as `entry.mindset`), cream accent (#D4C29B) distinct from action-oriented gold, `body[data-reflection-active]` CSS hook for Lora italic labels + larger 90px textareas, prompts opened to any timeframe ("What's been on your mind?" / "What is this stretch teaching you?" / "What are you carrying forward?"). Title optional. Commits `5fa26b6`, `5e8d799`.
- ✅ **Save Entry button feels like a button** — :active press state on every .btn site-wide (scale 0.97 + darker tint), saveEntry fires haptic + gold `savePulse` animation, universal pointerdown press-flash applied to every button / .btn / a.btn / [role=button] in the app. Commits `80ad87f`, `23a257a`.
- ✅ **Instant entry-list update on save — no refresh needed** — root cause was stale Pick Date / 7d / 30d filter silently excluding the new entry. Right after `state.entries.unshift`, force `_entriesFilter` to 'today' (or 'all' for back-dates), sync filter chip visual state, call renderEntries() directly. Last-resort fallback: if new entry-card not in DOM after render, force 'all' filter and re-render. requestAnimationFrame scrolls #entries-list into view + gold flash on new card. Commits `29b2886`, `187dde9`, `23a257a`, `f005e4d`.
- ✅ **Dashboard reorganized for parent-awareness** — removed Today's Plan card (redundant with Journal as action surface), merged Weekly Recap + Parents Portal into single "👨‍👩‍👦 Share With Family" card (Text + Email + Export PDF buttons + inline weekly body), lifted Coach Comms (New From Coach + Milestone) up under Academics, grouped Hitting Stats + Fielding (previously separated by Photobook), moved Photobook to sit next to Share With Family at the bottom. New top-to-bottom flow: Academics → Coach Comms → Month Focus → Training Volume → Gym → Rest Days → Goals → Hitting → Fielding → Photobook → Share With Family. Commit `6a975e3`.
- ✅ **Rest Days dashboard card** — counts offday-type entries with 7d / month / season buckets, "Last: N days ago" hint, blue accent (#5DADE2) matching Off Day badge palette, hidden until first Off Day exists. Wired into renderAll() so it updates the instant an Off Day is saved or deleted. Commit `0f8e3a1`.
- ✅ **Streak emoji tiers — 🧊 / 🔥 / 💎** — 🧊 frozen at 0.55 opacity (streak=0 AND <3 entries last 7d), 🔥 flame (any active streak OR 3+ entries last 7d), 💎 diamond (streak ≥ 7). Element stays visible at all tiers — cold state actively nudges instead of leaving a blank corner. Commit `f19af11`.
- ✅ **Age band broadened — MS through college** — Profile grade selector added 6th + 7th Grade options (was 8th-grade floor); School label "High School / Team" → "School / Team" with placeholder inclusive of MS/HS/college/travel; goal presets added Make My Middle School Team + Make the JV Team in Roster & Travel, 60+ / 70+ / 75+ MPH pitching tiers below the existing 80/85/90, Sub-8.0 / Sub-7.5 60-yard tiers below the existing Sub-7.0 / Sub-6.7. NCAA recruiting tiles stay as-is. Under-12 deferred to separate product post-launch. Commit `ab4a9b9`.
- ✅ **Modern font stack + touch accessibility audit** — body now ships the native font stack top mobile apps use (-apple-system, BlinkMacSystemFont, SF Pro, Segoe UI, Roboto, Inter, Barlow as legacy fallback) with -webkit-font-smoothing antialiased. Inter + Lora added to Google Fonts request. Three real mobile bugs fixed: input font-size 15px → 16px (kills iOS auto-zoom on focus), touch-action: manipulation on every interactive element (kills 300ms tap delay), -webkit-tap-highlight-color: transparent (removes grey flash on tap). 44px min-height tap floor already in place from 5-11 audit. Commit `c9a65bf`.
- ✅ **Arm Care hidden for Instructor entries + tip note** — instructor explicitly removed from arm-care visibility logic (`updateArmCareVisibility` + `hasThrowingEntryToday`). Tip note added at top of Instructor card: "Show up a little early so you can warm up before your lesson — arm care, light jog, dynamic stretches." Commit `51fc66c`.

### 2026-05-11 (Entry-type expansion + arm care rewrite + pre-launch audit — 14 commits, SW v78 → v92)
- ✅ **Off Day as a 6th entry type (Decision #28)** — 😴 added to the j-type dropdown. Form mirrors Reflection structure (no rep counter, no arm care, no game stats) but with off-day-specific placeholders ("Sunday rest · No throwing today · Family day"). Blue `.type-offday` pill in past entries. Lets a player journal even on no-training days without forcing a fake entry type. Commit `99e9d76`.
- ✅ **Per-entry sport tagging (Decision #29)** — every saved entry stamped with `entry.sport = 'baseball'|'softball'` at save time. `_filteredEntries` filters the entries list by active sport. Past entries without the tag (pre-5/11) are sport-agnostic and show in both views so legacy journals don't suddenly look empty. `editEntry` preserves the original sport. Commit `075308f`.
- ✅ **Sport tabs gated to owned sport** — non-owned sport tab is greyed + `aria-disabled`. `setActiveSport` refuses non-owned switches. Emoji walker no longer rewrites tab icons (softball tab keeps its 🥎 in baseball mode). Switching sport fires `cancelEdit()` to clear the entry form. Commit `ccacd2d`.
- ✅ **Entries-list filter chips** — `[📅 Today] [7 Days] [30 Days] [All] [📅 Pick Date]` above the past-entries list. Today is the default. Pick Date triggers the native phone calendar via `showPicker()` (`date:YYYY-MM-DD` filter format). Past-entries panel no longer pollutes the day-of view. Commits `1c54404`, `0a70a38`.
- ✅ **Streak rule locked (Decision #30)** — every entry type maintains the streak, including Off Day. Documented in a top-of-function comment on `calculateStreak()`. No code change needed since the function already iterates without type-filtering. Commit `6244d70`.
- ✅ **Prior-day delete safety upgraded** — three layers of friction now protect honest history: PIN → confirm modal with "📝 Edit Instead" escape → 10-second undo toast. Strong warning line ("⚠ THIS IS FROM N DAYS AGO") inside the modal for protected entries. Commit `46f85b0`.
- ✅ **Form date picker — back-date banner** — date input stays interactive (back-dating is a real "I forgot to log yesterday" need). Added a loud orange `📅 LOGGING FOR FRI, MAY 8, 2026 · 3 DAYS AGO` banner that activates the moment a non-today date is picked, so back-dating can't happen silently. After save, banner clears + form resets to today + toast confirms (`✅ Saved for May 8, 2026`). Commits `061641b` (lock attempt), `f3cfe81` (restore + banner), `0ef797c` (toast + auto-jump), `efa8be4` (revert auto-jump). Multiple iterations until the right model landed.
- ✅ **Arm Care content rewrite per Coach Young's throwing-day flow** — deleted Wrist Snaps. Added Run Poles, Arm Stretches, Long Toss, Jog After Bullpens, Ice With Pressure. Repurposed "Cool-down toss" → "Walk the ball in." Updated reference modal copy for every changed + new item. Sections: PRE-THROW WARMUP (poles → stretches → mobility → bands) · THROWING (toss → longtoss → walk-in) · RECOVERY (jog → ice → active rest). Removed tier-gating; full list always visible. Commits `cbef7a0`, `9f94c91`, `82262ce`.
- ✅ **Arm Care simplified to a daily reminder** — no mutual exclusion, no auto REST DAY pill. DONE pill at 5/5 + always-available `▲ Hide` button at any state. Coach: "good reminder, not mandatory." Multi-state pill (done / partial / empty) when manually collapsed. Commit `ccacd2d`.
- ✅ **Pre-launch hardening shipped** — photo compression (canvas resize to max 1200px + JPEG 0.82, typical 4MB phone photo → ~150-250KB; prevents silent localStorage cap data loss); 80%-of-cap warning toast; empty-state copy updated to mention Workout / Instructor / Off Day; Phase 7b V1 coach card wiring smoke-tested intact. Commit `6244d70`.
- 📌 **3 Decisions locked** — #28 (Off Day type), #29 (sport-tagged entries), #30 (streak counts every type).

### 2026-05-10 (Daily Journal walkthrough marathon — 28 commits, SW v51 → v78)
- ✅ **Schedule checklists 4/4 complete** — Calendar tab seasonal weekly content now renders as checkable bullets (190 bullets across 48 weeks, parsed via emoji-prefix detection, per-bullet state in `ybg_calendar_checks`). Commit `2150b61`.
- ✅ **Edit-PIN protection (Decision #25)** — entries older than 24 hours require a 4-digit PIN to edit or delete. First-time setup prompts; subsequent attempts verify against hashed `ybg_edit_pin`. 3 wrong attempts → 30-min lockout. Creator (Coach) bypasses. 🔒 icon + "edited Nh ago" badge in entry header. Commit `e85b3f4`.
- ✅ **Delete safety net** — replaced native browser confirm with a custom modal (🗑 Delete / 📝 Edit Instead / Cancel) + 10-second undo toast. Soft-delete holds the entry in memory; UNDO restores it to top of list. PIN-protected entries skip the modal — PIN is the confirmation. Commit `f49ad9a`.
- ✅ **Solo practice as first-class entry sub-type (Decision #26)** — practice context toggle [👥 With Team] [💪 On My Own]. Solo hides Coach Comment + Save Note for Coach (irrelevant), reveals multi-select solo work-type chips (🏌️ Tee / 🏏 Cages / ⚾ Bullpen / 🎯 Long Toss / 🧤 Fielding / 🏃 Running / 🤸 Mobility / 📋 Other). `entry.practiceMode='solo'` + comma-separated `entry.soloWorkType`. Past entries render as `individual · {work types}`. Title fallback: single-chip → label, multi → "Solo Practice." Commits `cca4c1d`, `6e40540`, `5f306f1`, `c2f73d1`, `62f7fb8`.
- ✅ **In-app coach/instructor notes (Decision #27)** — replaced "Text My Coach" SMS button (open-ended, no reply visibility) with always-visible in-app `entry.coachNote` + `entry.instructor.noteForCoach` fields. Caption: "Your coach will see it in the Coach Portal — coming with MyGrind Pro." Notes also surface in the weekly recap to parents. Two-way real messaging deferred to Phase 7b V2 as a paid upgrade. Commits `5d21976`, `ba74f22`, `fb90e7d`.
- ✅ **Future-date block + reps-without-context guard** — entry date picker hard-capped to today (timezone-safe via `todayLocalISO()`). Practice entries blocked from save with reps but no text/grade/photo/comment (prevents streak-padding). Commits `3066082`, `0872e18`.
- ✅ **Grade + Result chips replace dropdowns** — single-letter `<select>` options gave no context when closed. Now: `[A — Excellent] [B — Good] [C — Average] [D — Below Avg] [F — Struggled]` always visible; same chip pattern as the rep counter. Past-entry badges include the descriptor too: `[L · LOSS]` `[F · STRUGGLED]`. Commits `78eaf54`, `d29f46f`.
- ✅ **Typography hierarchy** — `.card-title` promoted from 13px Barlow Condensed eyebrow → 22px Bebas Neue display heading (affects all 40 card titles app-wide). Real H1 (42px) > H2 (22px) > body now. Rep counter chips bumped 12→15 + padding 7/10→11/14 (proper touch target). Past-entry rep tiles: numbers 16-22 → 22-28, labels 9 → 11. Commits `cc5e94e`, `f932753`, `8039bfe`.
- ✅ **Arm care victory pill** — once 5/5 done, the full checklist compresses to a green "Done for Today" pill (frees mobile real estate). Tap to re-expand; "▲ Hide" button to re-collapse. Commits `9172f96`, `2bfecc7`, `0f89180`.
- ✅ **Workout Volume counter on Dashboard** — 7-day / month-to-date / season buckets keyed off the `workout` entry type; "Last: 3 days ago" hint; hidden until first workout. Commit `39d6d9f`.
- ✅ **Past-entries section break** — replaced thin `<hr>` with a brand section header (📓 YOUR ENTRIES + Bebas 30px + gold accent line). Commit `65e7454`.
- ✅ **Rep counter polish** — Exact chip moved to first position (peer first-choice, not fallback); redundant "0" chip dropped (no-tap = 0 anyway). Commits `61f6c91`, `3312812`.
- ✅ **Form polish** — "+ NEW ENTRY" eyebrow stamp removed (redundant after card-title bump); Coach Comment subtitle dropped; date picker timezone bug fixed (was rolling to next day in evening Pacific via UTC `valueAsDate`); date resets to today after save. Commits `fb90e7d`, `f91bc1a`, `62f7fb8`.
- ✅ **Dead UI cleanup** — 5 orphaned functions/blocks removed (`showPaywall`, `saveAllStats`, `pullSeasonStats`, `dash-recent-entries` populator, `season-totals-display` block) + silent regression fix in PDF export (was reading dead `ybg_softball_stats` key; now derives season totals from journal game entries). Net –114 lines. Commit `907153e`.
- ✅ **Verified `setRepFromSavedValue()`** — Exact-mode fallback handles pre-2026-05-09 broader rep-chip buckets correctly. Comment added documenting the contract. Commit `2f24ee6`.
- 📌 **3 Decisions locked** — #25 (edit-PIN), #26 (solo as first-class), #27 (two-way coach deferred to MyGrind Pro).
- 🐛 **2 latent bugs caught + fixed** — date-picker timezone rollover, inline-style CSS specificity overriding `.selected` chip background.

### 2026-05-07
- ✅ **SEO foundation complete (NEW phase)** — title/description/OG/Twitter Card rewritten on `index.html` for "MyGrind app" + sport keywords. JSON-LD `@graph` with 3 nodes (Organization + WebSite + SoftwareApplication with 3 Offer prices). 1200×630 OG card at `/assets/og-card.png` generated via Canva using saved brand kit + web logo asset (LinkedIn preview verified). Vercel Web Analytics deployed across all 6 public pages (cookieless, privacy.html §11 compliant). Google Search Console URL-prefix verified via `/google826fc67b2dde2efa.html`; `sitemap.xml` updated with `lastmod` and submitted to Search Console with **Success** status. Commits `1513011`, `d49a88a`, `97b71b3`.
- ✅ **Signup flow polish** — removed redundant `<div class="badge">` from S1–S7 + redundant right-side prog-label spans across all signup screens (consistency: H1 already conveys section). Brightened `.field-label` + `.field-helper` colors; bumped global H1 (42→46, mobile 38→44) and `.sub` (15→16, mobile 17→18). Brand wordmark on Screen 0 promoted from small badge to a proper 26px gold wordmark. ⚡ icon added to Get Started CTA. Phone hardware back-button now navigates between screens via `history.pushState` + `popstate` listener. Promo-aware trial copy: when `MYGRIND6` or `FOREVERYOUNG2026` is entered on Screen 5, the sub line, "What happens at day 14?" promise block, and Screen 7 reminder line all rewrite to match the actual free window (6 months / lifetime); lifetime promos hide the "skip the trial" link entirely. Screen 7 "What's next" block resized to actually be seen. Screen 8 dashboard footer links bigger + brighter; modal typography enlarged; QR code option added to Share modal. Player-card preview brightened with gold-lock signal + 3 new engagement rows (coach feedback, season stats, split games/practices). Journal preview row reframed: visible count + "text stays private". Commits `673c355`, `2848144`, `75ba451`, `4bc9353`, `04f96e5`, `4c426e6`, `fd3df6e`, `8b10d67`, `996a894`.
- ✅ **Onboarding rebuilt with direct softball.html handoff** — `onFinalExit()` now navigates straight to softball.html (was: showed a stale "your dashboard is shipping soon" holding overlay; the entire `showFinalHoldingScreen` function + ~50 lines of dead code deleted). New "Open My Journal →" CTA on S14 replaces the old "Got It — I'll Be Back". Explicit journal-privacy promise added to S14 ("what you write in your journal stays private to you" — mirrors parent-side dashboard preview). Onboarding completion now writes `ybg_softball_access.trialStart = Date.now()` to localStorage so softball.html's `validateSession()` returns `'trial'` instead of bouncing to signup (Locked as **Decision #23**). Killed "You're In ⚾ Baseball" progress-bar text label across all 12 onboarding screens. Bumped "Now it's on you" headline on S13 (34→48px). Commits `fdc1017`, `fdbb8d1`, `d709d66`, `82a1293`, `15a9fab`, `6fed9dc`.
- ✅ **softball.html walkthrough started** — added `youngsbaseball@gmail.com` to `CREATOR_EMAILS` and a permanent `?unlock=1` URL-param creator bypass (handler runs at top of `init()` before the existing creator-flag cleanup, sets both `ybg_creator` AND `ybg_pin_unlocked`). Header logo enlarged + filtered to white (mark 44→60px, wordmark 32→42px). `.section-sub` bumped 13→16px and brightened. Daily Journal panel header cleaned (dropped redundant "Post-Game · Practice · Reflection" subhead + orphan gold-line). Commits `3330538`, `02b638b`, `473c2f3`, `34af5e1`.
- ✅ **`docs/ANALYTICS.md` shipped** — single reference doc covering Vercel Analytics dashboard, Stripe trial/paid views, Google Search Console performance, OpenGraph validator, weekly + monthly review checklists, and automation upgrade paths (weekly Notion pulse, Slack/email digest — both queued, neither built yet). Commit `df4cce3`.
- 📌 **Locked Decision #23** — Player onboarding completion auto-grants softball.html trial access. Previously the trial gate was separate from onboarding (double-gate caused tonight's redirect loop). Now: `onFinalExit()` writes `ybg_softball_access.trialStart` if not already paid/active. Trial-overlay welcome screen still exists for direct-link landings.

### 2026-05-06
- ✅ **Stripe webhook repaired + reconciled** — Vercel "Sensitive" env vars (STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PUBLISHABLE_KEY) had been saved with empty values during yesterday's signing-secret rotation, causing all webhook deliveries to return HTTP 500 `server_misconfigured`. Re-pasted via dashboard, redeployed, verified by Aaron Pulley's `customer.subscription.deleted` event auto-recovering to 200 OK at 7:34 AM. Patched all 4 silent-no-op `if (!email) break;` paths in `api/stripe-webhook.js` to `console.warn` with relevant Stripe IDs.
- ✅ **Path A reconciliation (Family pricing)** — discovered live Family Annual payment link charges $149.99/yr (not $149). Updated webhook PRICE_TO_PLAN map to live IDs (`price_1TT69tPm4...` family_monthly, `price_1TT68UPm4...` family_annual). Archived unused canonical `prod_US01TjY0XREZtc` "MyGrind Family" + 2 prices in Stripe. Family copy bumped $149 → $149.99 in 5 places in `signup.html` + 3 doc files (`CLAUDE.md`, `docs/ROADMAP.md`, `docs/STATUS.md`). Derived math fixed: `~$12.42/mo` → `~$12.50/mo`, `save $148/yr` → `save $147/yr`. Locked as **Decision #21**.
- ✅ **softball.html broken paywall fixed** — all 3 `STRIPE_*_URL` constants pointed at INACTIVE Stripe Payment Links (anyone tapping Subscribe got a "this link is no longer active" Stripe error). Updated to use signup.html's working URLs for Single Monthly + Single Annual; created new `plink_1TU78PPm4ermqky4nUSQdVmb` for $300 Team Sponsor.
- ✅ **Adult self-signup funnel (Scope X)** — `signup.html` now opens with a Screen 0 audience picker ("My Athlete" / "Myself"). Self path: `state.signupFor='self'` + `applySelfLabels(n)` swaps copy on Screens 1-4 + playerCount question hidden on Screen 3 + Screen 6 auto-fills player record from parent state and skips to Screen 7. URL handoff `?self=1` to `onboarding.html` triggers self-aware PIN screen, welcome subhead, end-of-flow copy. End-of-onboarding heading swapped from "See you tomorrow" → "Let's grind". Locked as **Decision #22**.
- ✅ **Multi-sport platform Phase 1** — `softball.html` refactored into a sport-aware shell themed per `data-sport`. Central `SPORTS` config (baseball/softball/both with id/label/slug/emoji/accent/accentRgb), `getSportConfig()` helper, exposed via `window.SPORTS` / `window.getSportConfig` / `window.mgSportConfig`. 127 `#D4547A` + 8 `#E87FA0` + 4 `#A03060` + 38 `rgba(212,84,122,X)` → `var(--gold)` family (165 sport-aware refs total). 16 hardcoded 🥎 → ⚾ as HTML defaults; 3 JS-context emoji uses (Notification × 2, drill icon) refactored to runtime `mgSportConfig.emoji`. Bidirectional `applyBallEmoji()`. New `window.setActiveSport(s)` global re-themes live (data-sport attr + theme-color meta + emoji walk + {Sport} template re-render). Multi-sport toggle UI in dashboard header (gold-pilled `⚾ Baseball | 🥎 Softball`, hidden by default, auto-revealed via CSS when `<html data-family-sport="both">`). New `mg_family_sport` localStorage key tracks original signup choice. signup.html `selectFamilySport()` writes the same key so dashboard knows when to surface the toggle. Recipe doc `docs/ADD_A_SPORT.md` makes new sports a ~15-minute job. "Coming soon for: 🏈 🏐 🏀 ⚽" strip on Screen 0 signals expansion. Locked as **Decision #20**.
- ✅ **Promo code system** — collapsible `+ HAVE A PROMO CODE?` gold-pilled button on Screen 5 of `signup.html`. Generic "ENTER CODE" placeholder. Auto-uppercases. Persists to `state.promoCode`. New shared `buildCheckoutUrl()` helper appends `prefilled_promo_code=` + `prefilled_email=` to Stripe Payment Link URL. Both checkout entry points (`onSkipTrialClicked` + `onDashboardPayClicked`) use it. Restore logic on Screen 5 entry pre-fills + auto-expands the dropdown if a code is saved.
- ✅ **`FOREVERYOUNG2026` lifetime free code created** — Stripe coupon `CQoNpVpK` (100% off, duration: forever), promotion code `promo_1TUIriPm4ermqky4RpZVI42Y`. Capped at **10 redemptions**, expires 2030-12-31. For hand-picked recipients. Card still collected at Stripe checkout but never charged.
- ✅ **Trial banner removed from Screen 8 dashboard** in `signup.html`. "14 Days Free" was redundant (parent saw it twice already on Screens 1+5) + risk of any player picking up the parent's phone seeing billing copy.
- ✅ **Defensive horizontal-scroll CSS** in `signup.html` — `min-width: 0` on `.app`/`.screen`/`.screen-body` (fixes flex-child auto-min-width gotcha). `body { max-width: 100vw }`. Plus `img/video/iframe { max-width: 100% }`.
- ✅ **Logo + font bumps** for adult readability on phone — logo 96px → 140px height + 80vw → 90vw on `signup.html` + `onboarding.html`. Phone-only @media block bumps `.sub` 16→17, `.tap-card-desc` 13→15, `.goal-toggle-text` 14→16, `.field-input` 16 (kills iOS zoom-on-focus). Same media block added to `onboarding.html`.
- ✅ **`docs/ADD_A_SPORT.md` recipe doc** — 5-step guide (SPORTS entry + CSS block + signup tile + optional content bundle + optional landing page) with checklist, existing-sports table, and "what won't break per-sport" notes. Targets ~15-minute time-to-add for football, volleyball, basketball, soccer, etc.

### 2026-05-05
- ✅ **Phase 5 Steps 2-4 — Stripe webhook + dashboard pay + subscription sync.** New `api/stripe-webhook.js` (signature-verified, handles 5 event types), new `api/get-subscription.js`, new `lib/subscription-store.js` (Redis CRUD by email, idempotent on `lastEventId`). Dashboard "Pay early" link added to signup.html Screen 8. softball.html now syncs paid status from server on every app load. Webhook destination "fascinating-oasis" live in Stripe Workbench. Commit `bbe6cbf`.
- ✅ **Phase 7b V1.5 — parent weekly email digest** shipped end-to-end and verified by real Gmail delivery. New `lib/email-digest.js` (warm-dark branded HTML, groups by player), new `api/cron/weekly-digest.js` (Vercel cron, SCANs Redis for active parents, sends via Resend, auth via `Bearer ${CRON_SECRET}`), new `vercel.json` (Monday 14:00 UTC schedule). Test mode: all emails redirected to youngsbaseball@gmail.com via `WEEKLY_DIGEST_TEST_EMAIL` env until `mygrindapp.com` is verified in Resend. Sender: `onboarding@resend.dev`. Commits `8881d38`, `bd03455` (chip rendering fix for `{key,label,detail}` objects).
- ✅ **mygrind-visuals skill** added at `~/.claude/skills/mygrind-visuals/SKILL.md`. Orchestrates Higgsfield image + video generation with MyGrind brand guardrails (Warm Dark palette, parent-first voice, banned-words list, model decision tree, prompt template). Auto-fires on any MyGrind visual request.
- ✅ **MyGrind logos uploaded to Higgsfield** — 4 variants (master 5692×3200, web 920×517, mark 902×907, app icon 179×180). Media IDs saved to memory `higgsfield_mygrind_media.md` and wired into the skill so generations default to the right logo for context.
- ✅ **Launch creative generated** — 2× kling3_0 Reels (player finishing BP → opens MyGrind on phone, with voiceover and ambient sound), 2× nano_banana_2 photorealistic IG posts (14-yo boy journaling in dugout at golden hour). Job IDs visible in Higgsfield generations panel.

### 2026-05-04
- ✅ **Phase 7b V1 — end-to-end coach feedback loop** shipped + verified against production. Player → SMS magic link → coach taps → coach-reply.html → coach picks chips + comment → SMS player + parent weekly review card on dashboard. NEW: `lib/feedback-store.js` (Redis-backed CRUD), 4 API endpoints (`feedback-request`, `feedback-get`, `feedback-respond`, `feedback-list`), `coach-reply.html`. Updates: softball.html "💬 New from Coach" dashboard card, signup.html Screen 8 "📋 This Week's Coaching" parent card. Token-gated writes (32-hex secret), 90-day TTL. SMS via Twilio (DRY_RUN until TFV approves). Commit `c00d3a9`.
- ✅ **Phase 7a — player Coach Feedback tab** shipped (mailto V1) earlier in the day; superseded by 7b V1. Commit `bb74304`.
- ✅ **Twilio TFV submitted** 2026-05-04 via console. Privacy §10 + Terms §9 added with full SMS compliance language (consent, frequency, STOP/HELP, retention, carrier disclosure, parent authority for player phone). Calendar check-in May 11 9am PT. `SMS_DRY_RUN=true` stays until Twilio shows APPROVED. Commit `b277a45`.
- ✅ **Logo brand cleanup** — replaced text "MY GRIND" / "MG" with real logo assets across the entire app. `assets/apple-touch-icon.png` + `assets/favicon.png` regenerated from current `mark.png` (was old red/black design); inline base64 for splash + header re-encoded too. Commits `3b8f48c`, `a9978b9`, `6a5c5db`.
- ✅ **All customer-facing email → support@mygrindapp.com** — 59 replacements across signup/softball/terms/privacy + CREATOR_EMAILS access list (admin login flips to support@). Backup admins (papamike@, michael@youngsbaseball.com) preserved. Commit `b92d7dc`.
- ✅ **P0 security audit complete** — CREATOR_PIN backdoor (was hardcoded "YBG2026") removed from softball.html; CORS allowlist locks `/api/send-invite` to mygrindapp.com origins only; Firestore rules verified auth-required + uid-scoped; `SMS_DRY_RUN=false` confirmed missing in Vercel Production (intentional for pre-TFV). Commit `e74e05d`.
- ✅ **Phase 2 — Sport-aware copy** in softball.html. Title/meta/OG/body all swap on `?sport=` via head-script template system (`data-mg-text` attribute + `window.mgSportName`). Sport-aware ⚾↔🥎 emoji swap on softball.html + onboarding.html. Commit `d8cf6c5`, `c7f117c`, `7f98d7a`.
- ✅ **Screen 8 stale-player-cards bug fixed** — 5th render path with the same trim bug as Screens 6/7 (selectPlayerCount, buildScreen7, buildPhoneConfirmList, getFallbackPlayerNames). Now slices `state.players` to `playerCount` at render time. Commit `4f6aa32`.
- ✅ **YBG → MyGrind newsletter merge** decision locked. Issue #1 of "The Grind Weekly" drafted in Notion (`345819955e6f817181bdd396d9fb1c41`) ready to send 2026-05-05 7am PT — announces the brand merge at top, includes full MyGrind app intro + pricing + MYGRIND6 promo, scout/recruiting training topic, 60-yard dash drill, new permanent "For the Parents" section, YBG travel team CTA, Pocket Radar gear pick. After send: YBG Mailchimp audience absorbed into MyGrind audience tagged `ybg-legacy`, YBG audience archived.

### 2026-05-03
- ✅ **Phase 5 Step 1 — Skip-trial Stripe redirect wired** in `signup.html`. `onSkipTrialClicked()` redirects to one of 4 live Payment Links (Single/Family × Annual/Monthly) based on `state.preferredPlan` + `state.playerCount`, with `?prefilled_email=` from the parent's typed email. Steps 2-4 (webhook + dashboard pay flow) deferred pending baseball-side fix. (uncommitted in working tree)
- ✅ **Baseball Side Phase 1 — palette swap** in `softball.html`. Sport-detection script in `<head>` reads `?sport=` URL param → `mg_sport` localStorage → default baseball, sets `data-sport` on `<html>` synchronously before paint, updates theme-color meta. `:root` CSS variables (`--gold`, `--accent-rgb`) with `[data-sport="softball"]` override. Splash circle + diamond pattern bg now swap with sport. (uncommitted)
- ✅ **Baseball Side Phase 3 — per-player sport handoff** across `signup.html`, `api/send-invite.js`, and `onboarding.html`. Screen 6 in signup gets a per-player sport tile picker (Baseball / Softball / Both) shown only when `familySport === 'both'`; otherwise auto-derives from family sport. `buildSmsForPlayer(player)` builds invite URL with `&sport=`, matching `api/send-invite.js`. `onboarding.html` mirrors the Phase 1 detection script + `:root` palette tokens; 33 `#C9A84C` → `var(--gold)`, 11 `rgba(201,168,76,…)` → `rgba(var(--accent-rgb), …)`. `parseUrlParams()` accepts `?sport=baseball|softball|both`; `firstFlowScreen()` skips S1 sport-picker when sport is pre-filled. `setSport()` (when kid does pick) propagates to `data-sport` + localStorage + theme-color. (uncommitted)
- ✅ **YBG branding removed** from visible UI in `softball.html`. 8 `>YBG</div>` instances → `>MG</div>` (splash, header, team logos). Team-create placeholders updated to generic examples (`Lions Travel 14U`, `ABC`, etc.). Default team name `YBG TRAVEL` → `YOUR TEAM`. Fallback abbreviation `'YBG'` → `'MG'`. Internal references intentionally kept: `ybg_*` localStorage keys (would break testers), `CREATOR_PIN = 'YBG2026'` (admin access), `YBG_QOTD` variable, console prefixes, cache name. (uncommitted)
- ✅ **State trim bug fixes** in `signup.html`. `selectPlayerCount()` now trims `state.players` when count drops. Render-time slice in `buildScreen7()`, `buildPhoneConfirmList()`, `getFallbackPlayerNames()` so stale slots from prior sessions don't leak into invite previews (visible bug: confirm modal showed Lucas + Sofia when only 1 player picked). (uncommitted)
- ✅ **`?reset=1` URL handler** in `signup.html` for clean test runs — wipes `mg_signup_state`, `mg_sport`, `ybg_creator` and lands at Screen 1. Strips the param from URL bar after wipe. (uncommitted)
- ✅ **SEO baseline** — H1 hardcoded, canonicals (www), noindex on funnel pages, robots.txt, sitemap.xml. Commits `c864e00` + `e238322`.
- ✅ **Phase 5 preflight 🟢 GO** — verified Stripe account, all 6 prices live, webhook endpoint configured, all 3 Stripe env vars set in Vercel Production. Two non-blocking flags surfaced: 2 duplicate Family products to archive in Stripe dashboard, 3 legacy Stripe URLs in `softball.html` (lines 3662-3664) need separate audit.
- ✅ **Locked Decision #16** — "Both" sport means a single multi-sport athlete (one kid who plays baseball AND softball), NOT a family with mixed-sport kids. Refines Decision #7.
- ✅ **Pre-launch infrastructure** (morning marathon): `/morning-sync`, `/eod-sync`, `/save-prompt`, `/publish-prompts`, `/phase5-preflight`, `/twilio-tfv-prep` skills built. 73 read-only MCP tools pre-approved. Scheduled tasks armed (5:10am + 9:08pm PT). Drive prompt library shipped. Canva launch carousel V2 (10 slides) on Coach's Canva account. MyGrind Family product + 4 Payment Links + `MYGRIND6` promo (250 cap, expires 2026-09-01) wired in Stripe.

### 2026-05-02
- ✅ **Family pricing tier shipped** — added a $149.99/yr · $14.99/mo flat household plan covering 2-3 players, alongside the existing Single tier ($99/yr · $9.99/mo per player). Screen 5 pricing cards render dynamically via `renderPricingCards()` based on `state.playerCount` (1 → Single, 2-3 → Family) with a "You Save $X" line below each Family card. Anti-abuse defenses: per-plan phone uniqueness check (no two players share the same real number; fallback exempted), slot-lock fires on first SMS-send (Screen 7 entry → `state.playersLockedAt` ISO timestamp), navigating back to Screen 6 after lock shows a read-only view with a gold banner pointing users to `coach@mygrindapp.com` for swaps. Settings modal updated to explain the swap-via-email policy. Stripe URLs deferred to Phase 5.
- ✅ **Phase 6 — Real Share + Settings modals on signup dashboard** — replaced the two `alert('coming in Phase 6')` placeholders on Screen 8 with proper modals reusing the existing `.phone-confirm-overlay` shell. Share modal has a copy-link button (Clipboard API + execCommand fallback), a Web Share API "Share…" button shown only on supporting browsers, and a pre-written share caption. Settings modal shows read-only account summary (name, email, phone, plan, players) populated from `state.parent` + `state.preferredPlan` + `state.playerCount`, plus a `mailto:coach@mygrindapp.com` link for change requests until Phase 5 wires backend editing.
- ✅ **Phase 3d — Twilio Lookup pre-check** — new `lib/lookup.js` module + integration in `api/send-invite.js`. Calls Twilio Lookup v2 with `line_type_intelligence` between E.164 normalization and rate-limit checks. Rejects invalid numbers, landlines, and VoIP with friendly user-facing messages before burning rate-limit budget or paid SMS sends. Fail-open on Lookup outage. `SKIP_LOOKUP=true` env override for dev. Cost ~$0.005/lookup, bounded by upstream rate limits.
- ✅ **Pre-launch waitlist landing page** at `mygrindapp.com` — brand-styled `index.html` with Mailchimp form (audience `73280b4c02e9bc56c7e633892`, tag `2216797`). Replaced the old redirect shell that bounced visitors to signup.html.
- ✅ **Mailchimp JSONP integration** — replaced optimistic iframe submit with real `subscribe/post-json` call so users see actual success/error messages instead of silent failures.
- ✅ **Mobile sizing fixes for landing** — compact email input (34px tall), narrower form card on portrait, landscape media query, vertical centering with `safe center`, iOS Safari rotation-zoom fix via `maximum-scale=1`.
- ✅ **Disabled Mailchimp reCAPTCHA on the form** so JSONP path doesn't bounce real users into a challenge that can't render.
- ✅ **Critical CSS corruption fix in `onboarding.html`** — `.screen`, `.screen.on`, `.screen-body`, `.prog-wrap` rules had nested braces and orphan property remnants from a prior find/replace incident. Restored intended layout.
- ✅ **`saveState()` data-wipe killed** in `softball.html` — the catch block was silently writing `entries: []` on quota errors. Now leaves previous save intact and surfaces a one-time alert pointing to backup.
- ✅ **GameChanger Option 1 disabled** — the screenshot reader called Anthropic API directly from the browser (CORS-blocked, no key). Replaced with "Coming Soon" notice pointing to Option 2 (manual entry).
- ✅ **Backup & Restore feature re-shipped** in `softball.html` Settings tab — JSON download/upload of all `ybg_*` keys (auth/session keys denylisted). Includes "last backup: X days ago" display.
- ✅ **Backup nudge re-shipped** — non-blocking gold banner appears on app load when user has 3+ entries AND no backup in 7+ days. Dismissable for the session.
- ✅ **Brand system doc** at `docs/BRAND_SYSTEM.md` documenting design tokens for future MyGrind web surfaces.
- ✅ **`media/` folder** added for launch graphics + social assets.
- ✅ **STATUS.md + ROADMAP.md rewritten** to reflect current architecture (signup/onboarding/softball/landing) instead of the old single-file index.html state.

### Earlier (rolled into this list when I clean up)
- ✅ Tab reorder (daily-use features up front)
- ✅ Instructor Session log in Daily Journal
- ✅ Daily affirmation banner (60 affirmations rotating)
- ✅ Instructor as entry type (focused form when selected)
- ✅ Universal "NO GRADES · NO GAME" banner (sport-neutral)
- ✅ Pink palette for softball mode
- ✅ 20 softball-specific quotes
- ✅ Custom academic classes
- ✅ Cloud backup (manual Save to Cloud)
- ✅ Sport selector on trial signup
